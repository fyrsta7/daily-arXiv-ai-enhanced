<div id=toc></div>

# Table of Contents

- [cs.SE](#cs.SE) [Total: 12]
- [cs.AI](#cs.AI) [Total: 17]
- [cs.LG](#cs.LG) [Total: 6]
- [cs.CL](#cs.CL) [Total: 5]
- [cs.CY](#cs.CY) [Total: 1]
- [cs.RO](#cs.RO) [Total: 2]
- [cs.CV](#cs.CV) [Total: 1]
- [cs.HC](#cs.HC) [Total: 1]
- [cs.CR](#cs.CR) [Total: 4]
- [cs.IR](#cs.IR) [Total: 2]
- [cs.DC](#cs.DC) [Total: 2]
- [cs.GT](#cs.GT) [Total: 1]


<div id='cs.SE'></div>

# cs.SE [[Back]](#toc)

### [1] [AgentMeter: Evaluating Model-CLI Matching for CLI-Based Local Task-Solving Agents](http://arxiv.org/abs/2606.21140v1)
*Han Chi,Jiaxin Qi,Yan Cui,Baisheng Lai,Jianqiang Huang*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 提出AgentMeter基准和AMS指标，用于评估CLI介导的本地任务求解智能体中模型与CLI的匹配，证明模型和CLI选择应作为整体评估。


<details>
  <summary>Details</summary>
Motivation: 现有评估仅强调任务成功，忽略了CLI介导对整个系统的影响，导致相同模型在不同CLI下表现差异显著，因此需要将模型和CLI作为整体进行评估。

Method: 引入AgentMeter基准测试，包含Benchmark90完整验证集和Core30低成本子集，并设计AMS指标（成功导向、成本感知）。在24个模型-CLI配置上进行比较。

Result: 不同部署标准（如Pass/30、Token/Pass、成本/Pass、AMS）选出不同最优配置；Benchmark90验证保持Top-1和Top-3配置，Spearman相关0.765，Kendall相关0.567，AMS MAE 0.0383。

Conclusion: 模型和CLI选择不应解耦，模型-CLI配置应作为部署单元进行评估。

中文摘要：LLM 智能体越来越多地通过命令行和基于CLI的测试工具接口解决本地任务，包括代码编辑、仓库检查、数据分析以及文件工作流。现有的评估通常强调任务成功，但部署的本地智能体不仅仅是模型：CLI 介导了提示词、上下文重放、工具输出、文件访问、终端观察和停止行为。因此，相同的模型在不同的CLI下可以产生不同的成功、token和成本情况。我们引入了 AGENTMETER，一个用于评估CLI介导的本地任务求解智能体中模型-CLI匹配的基准测试，同时提出了 AgentMeter 分数（AMS），一种基于成功、考虑成本的度量标准，建立在经过校准的任务工作量层级上。AgentMeter 使用 Benchmark90 作为完整验证集，Core30 作为低成本子集，用于在24个完整的模型-CLI配置上进行扩展比较。在 Core30 上，常见的部署标准会选择不同的配置：最高的 Pass/30 选择带有 qwen-coder 的 GLM-5.1，最低的 Tok./Pass 选择带有 kimi-cli 的 GPT-5.3-Codex，最低的可计费美元/Pass 选择带有 Codex 的 Qwen3.6+，而最高的 AMS 选择带有 kimi-cli 的 Qwen3.6+。Benchmark90 验证保留了 Top-1 配置和 Top-3 集合，Spearman 相关系数为 0.765，Kendall 相关系数为 0.567，AMS MAE 为 0.0383。这些结果表明，模型选择和CLI选择不应解耦，并且模型-CLI配置应该作为部署单元进行评估。

Original abstract: LLM agents increasingly solve local tasks through command-line and CLI-based harness interfaces, including code editing, repository inspection, data analysis, and file workflows. Existing evaluations often emphasize task success, but deployed local agents are not models alone: the CLI mediates prompts, context replay, tool outputs, file access, terminal observations, and stopping behavior. As a result, the same model can produce different success, token, and cost profiles under different CLIs. We introduce AGENTMETER, a benchmark for evaluating model-CLI matching in CLI-mediated local task-solving agents, together with AgentMeter Score (AMS), a success-anchored, cost-aware metric over calibrated task-effort tiers. AgentMeter uses Benchmark90 as the full validation set and Core30 as a lower-cost subset for expanded comparison across 24 complete model-CLI configurations. On Core30, common deployment criteria select different configurations: highest Pass/30 selects GLM-5.1 with qwen-coder, lowest Tok./Pass selects GPT-5.3-Codex with kimi-cli, lowest billable USD/Pass selects Qwen3.6+ with Codex, while highest AMS selects Qwen3.6+ with kimi-cli. Benchmark90 validation preserves the Top-1 configuration and Top-3 set, with Spearman correlation 0.765, Kendall correlation 0.567, and AMS MAE 0.0383. These results show that model choice and CLI choice should not be decoupled, and that model-CLI configurations should be evaluated as the deployed unit.

</details>


### [2] [Context-Aware Generative AI for Automated Telecom Test Script Generation](http://arxiv.org/abs/2606.21151v1)
*Gautam Prasad,Chandramohan T. N.,Joy Bose*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 提出一个上下文感知的生成式AI框架，通过实时知识图谱和增量条件测试生成，实现电信测试的持续自适应。


<details>
  <summary>Details</summary>
Motivation: 现有测试生成方法生成静态测试套件，随着系统演变而过时，缺乏持续检测细粒度变化并选择性更新测试的解决方案。

Method: 使用持续更新的知识图谱作为单一事实来源，增量引擎检测变化，KG引导的生成式AI代理（通过MCP）自动创建/更新/废弃测试，并集成RAG丰富推理。

Result: 在软件系统和电信网络用例中（如KPI监控应用）展示了减少手动工作量、提高测试相关性、加速测试周期。

Conclusion: 该框架有效解决了测试过时问题，实现了持续自适应测试生成。

中文摘要：电信软件系统和网络的自动化测试生成随着机器学习和基于规则的方法的采用而取得了显著进展。然而，大多数现有解决方案针对系统的快照生成静态测试套件；随着代码、配置、拓扑和关键绩效指标（KPI）的演进，这些测试很快变得过时或与实时系统不一致。目前尚无广泛采用的解决方案能够持续检测细粒度变化并仅选择性调整受影响的测试，而无需重新生成整个测试套件。本文提出了一种上下文感知的生成式AI框架，用于自动化电信测试脚本生成，该框架将测试视为一个由系统当前状态驱动的持续自适应过程，而非静态产物。核心贡献是基于实时知识图谱的增量条件测试生成：我们的方法采用持续更新的知识图谱（KG）作为单一事实来源、一个用于细粒度变化检测的增量引擎，以及一个通过模型上下文协议（MCP）运行的KG引导的生成式AI代理，来自动创建、更新或废弃测试用例。我们进一步集成检索增强生成（RAG），以利用电信领域知识和历史工件丰富推理。我们展示了在软件系统和电信网络用例中的适用性，包括一个在GitLab中管理的基于Python的KPI监控应用程序，并展示了该框架如何减少手动工作量、提高测试相关性并加速测试周期。

Original abstract: Automated test generation for telecom software systems and networks has advanced significantly with the adoption of machine learning and rule-based approaches. However, most existing solutions generate static test suites against a snapshot of the system; as code, configurations, topologies, and key performance indicators (KPIs) evolve, these tests quickly become outdated or misaligned with the live system. There is currently no widely adopted solution that continuously detects fine-grained changes and selectively adapts only the affected tests without regenerating entire test suites. This paper presents a context-aware generative AI framework for automated telecom test script generation that treats testing as a continuously adapting process driven by the current state of the system rather than a static artifact. The central contribution is delta-conditioned test generation over a live knowledge graph: our approach employs a continuously updated knowledge graph (KG) as a single source of truth, a delta engine for fine-grained change detection, and a KG-guided generative AI agent, operating via the Model Context Protocol (MCP), to create, update, or retire test cases automatically. We further integrate Retrieval-Augmented Generation (RAG) to enrich reasoning with telecom-domain knowledge and historical artifacts. We demonstrate applicability across software-system and telecom-network use cases, including a Python-based KPI monitoring application managed in GitLab, and show how the framework reduces manual effort, improves test relevance, and accelerates test cycles.

</details>


### [3] [An Exploratory Case Study of LLM-Assisted Refactoring and Gameplay Feature Generation in an Endless Runner Game](http://arxiv.org/abs/2606.21171v1)
*Jan Wunderlich,Markus Kleffmann,Sebastian Lempert*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: GPT-4o在游戏开发中重构任务表现良好，但生成新玩法特征可靠性较低。


<details>
  <summary>Details</summary>
Motivation: 探索LLM在游戏开发中集成代码的实用性，特别是对于现有系统的重构和特征生成。

Method: 对GPT-4o在Python/Pygame无尽跑酷游戏中进行案例研究，包含六个任务（三个重构、三个特征生成），通过度量、测试和评估。

Result: 所有重构任务成功，仅一个特征生成任务成功集成。

Conclusion: GPT-4o在局部重构上更可靠，但跨系统交互生成挑战大；观察为指示性。

中文摘要：大型语言模型（LLM）越来越多地被用于支持软件开发，但它们在应用游戏开发环境中的实际实用性仍未得到充分探索，尤其是在生成的代码必须集成到现有游戏软件系统时。本文对GPT-4o在一个自定义Python/Pygame无尽跑酷游戏中进行了探索性实证案例研究。该研究考察了六个选定的开发任务：三个局部重构任务和三个涉及游戏玩法特征生成的任务。结果实现通过软件度量、单元测试和手动游戏评估进行了评估。在本案例研究中，所有三个选定的重构任务在功能上均成功完成，而三个选定的游戏玩法特征生成任务中只有一个产生了正确集成的特征。研究发现表明，在这种设置下，GPT-4o处理局部转换比处理需要跨多个现有系统的新游戏交互的任务更可靠。鉴于探索性的单案例设计，这些结果最好解释为指示性观察，而非类别级模型性能的可推广证据。总体而言，本文提供了一个透明的基于案例的叙述，阐述了在现有游戏软件系统中LLM辅助重构和游戏玩法特征生成的机会与局限。

Original abstract: Large language models (LLMs) are increasingly used to support software development, but their practical usefulness in applied game-development settings remains underexplored, especially when generated code must be integrated into an existing game software system. This paper presents an exploratory empirical case study of GPT-4o in a custom Python/Pygame endless runner. The study examines six selected development tasks: three localized refactoring tasks and three tasks involving gameplay feature generation. The resulting implementations were evaluated using software metrics, unit tests, and manual gameplay assessments. In this case study, all three selected refactoring tasks were completed successfully in functional terms, whereas only one of the three selected gameplay feature generation tasks resulted in a correctly integrated feature. The findings suggest that, in this setting, GPT-4o handled localized transformations more reliably than tasks requiring new gameplay interactions across multiple existing systems. Given the exploratory single-case design, these results are best interpreted as indicative observations rather than as generalizable evidence of category-level model performance. Overall, the paper contributes a transparent case-based account of the opportunities and limitations of LLM-assisted refactoring and gameplay feature generation in an existing game software system.

</details>


### [4] [Change Impact Recommendation for JavaScript: Lessons from History and Runtime Analysis](http://arxiv.org/abs/2606.21187v1)
*Sadjad Tavakoli,Saba Alimadadi*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 结合动态分析和历史分析的混合方法能更好预测JavaScript代码变更影响。


<details>
  <summary>Details</summary>
Motivation: JavaScript动态特性使依赖推断困难，现有基于依赖或历史的方法各有局限，需要研究其优劣。

Method: 实现了三种推荐技术（历史共变模式挖掘、动态依赖分析、混合方法）在Caprese框架中，并在10个Node.js应用上使用专家参考集评估。

Result: 历史与动态分析候选重叠仅22%，动态精度更高，历史发现额外相关候选。

Conclusion: 实际变更影响推荐应结合运行时和演化信号，单一技术不足以覆盖所有相关候选。

中文摘要：理解代码变更的下游影响对于软件维护、调试和回归测试至关重要。这一任务对于JavaScript应用程序尤为具有挑战性，因为动态语言特性（如回调、事件、异步执行和共享可变状态）使得依赖关系难以精确推断。现有的变更影响推荐方法主要依赖于基于依赖的分析或仓库挖掘。基于依赖的技术，特别是动态分析，从观察到的执行中捕获运行时交互，但可能遗漏在分析期间未执行的关系。相比之下，基于历史的技术从过去的变更中揭示演化耦合，但由于噪声共变模式常常引入不精确的推荐。为了研究这些方法在JavaScript中的优势和局限性，我们设计并评估了三种推荐技术：一种基于历史的方法（使用共变模式挖掘）、一种基于动态依赖的方法，以及一种结合两种信号的混合方法。我们在一个统一的框架Caprese中实现了这些技术，并使用专家策划的参考检查集在10个开源Node.js应用程序上进行了评估。我们的结果显示，基于历史和动态分析识别的候选对象重叠度较低，在更广泛的检查预算下重叠率仅为22%，表明这两种方法捕获了互补的影响信号。动态分析通常产生更高的精确度，而基于历史的分析则识别出依赖分析遗漏的其他相关候选对象。这些发现表明，JavaScript中的实际变更影响推荐受益于结合运行时和演化信号，因为单一技术不足以捕获所有相关的检查候选对象。

Original abstract: Understanding the downstream effects of code changes is essential for software maintenance, debugging, and regression testing. This task is particularly challenging for JavaScript applications, where dynamic language features such as callbacks, events, asynchronous execution, and shared mutable state make dependencies difficult to infer precisely.
  Existing change impact recommendation approaches rely primarily on either dependency-based analysis or repository mining. Dependency-based techniques, particularly dynamic analysis, capture runtime interactions from observed execution but may miss relationships not exercised during analysis. In contrast, history-based techniques uncover evolutionary coupling from past changes but often introduce imprecise recommendations due to noisy co-change patterns.
  To investigate the strengths and limitations of these approaches in JavaScript, we engineer and evaluate three recommendation techniques: a history-based approach using co-change pattern mining, a dynamic dependency-based approach, and a hybrid approach combining both signals. We implement these techniques in a unified framework, Caprese, and evaluate them on 10 open-source Node.js applications using expert-curated reference inspection sets.
  Our results reveal low overlap between candidates identified by history-based and dynamic analyses, with only 22% overlap at broader inspection budgets, indicating that the two approaches capture complementary impact signals. Dynamic analysis generally yields higher precision, while history-based analysis identifies additional relevant candidates missed by dependency analysis. These findings suggest that practical change impact recommendation in JavaScript benefits from combining runtime and evolutionary signals, as no single technique sufficiently captures all relevant inspection candidates.

</details>


### [5] [KBSpec: LLM-driven Formal Specification Generation with Evolving Domain Knowledge Base](http://arxiv.org/abs/2606.21339v1)
*Wenhan Wang,Zeyu Sun*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: KBSpec通过融合外部文档知识和内部验证反馈构建自进化知识库，无需参数调优或标注数据，显著提升LLM生成形式规约的验证通过率和高完整性规约数量。


<details>
  <summary>Details</summary>
Motivation: 现有LLM生成形式规约因缺乏语料库导致语法和语义错误，需要一种无需大量标注数据的方法来提升规约质量。

Method: 提出KBSpec，利用官方文档作为外部知识和验证器反馈作为内部知识，构建自进化知识库，持续从成功生成和修复轨迹中更新，无需LLM参数调整或标注数据。

Result: 在JML规约生成任务上，使用三个LLM后端，KBSpec将验证通过率提高10-25%，并生成最多的高完整性规约。

Conclusion: KBSpec有效提升了LLM生成形式规约的语法正确性和语义可验证性，无需额外训练数据，是一种实用的增强方法。

中文摘要：自动化形式规约生成是向程序理解和形式验证迈进的关键步骤。近年来，由于大语言模型（LLMs）在代码生成方面的成功，研究人员已初步尝试采用LLMs生成形式规约。然而，现实世界中缺乏形式规约语言语料库，常常导致LLMs无法生成语法正确且语义可验证的规约。为弥补这一不足，我们提出KBSpec，它通过双源知识增强LLMs：来自官方文档的外部知识，以及从验证器对LLM生成的规约的反馈中提取的内部知识。KBSpec维护一个自我进化的知识库，该知识库从成功生成和修复轨迹中持续更新，无需任何LLM参数调整或标注训练数据。我们在Java建模语言（JML）规约生成任务上对KBSpec进行了评估，使用了三个LLM后端，结果表明，与最先进的基于LLM的方法相比，KBSpec将验证通过率提高了10-25%，同时产生了最多的高完整性规约。

Original abstract: Automated formal specification generation is a key step towards program understanding and formal verification. Recently, due to the success of large language models (LLMs) in code generation, researchers have made early attempts to adopt LLMs for generating formal specifications. However, the lack of formal specification language corpora in the wild often makes LLMs fail to generate syntactically correct and semantically verifiable specifications. To mitigate this gap, we propose KBSpec, which augments LLMs with dual-source knowledge of formal specification language: external knowledge from official documentation, and internal knowledge distilled from verifier feedback on LLM-generated specifications. KBSpec maintains a self-evolving knowledge base that is continuously updated from successful generation and repair trajectories, without any LLM parameter tuning or labeled training data. We evaluate KBSpec on Java Modeling Language (JML) specification generation with three LLM backends, and results show that KBSpec improves verification pass rates by 10-25% over state-of-the-art LLM-based approaches, while producing the largest number of high-completeness specifications.

</details>


### [6] [ATLAS: Agentic Taxonomy of Large-Scale Software Ecosystems](http://arxiv.org/abs/2606.21597v1)
*Junyi Lu,Mengyao Lyu,Jiahui Wu,Lei Yu,Chengwei Liu,Fengjun Zhang,Li Yang,Chun Zuo,Yang Liu*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: ATLAS通过结合LLM知识和仓库分布，自动构建层次分类体系，在GitHub仓库分类上达到83.13%的TQF，优于所有基线，并揭示了生态系统趋势。


<details>
  <summary>Details</summary>
Motivation: GitHub现有的Topics机制是扁平且不一致的，无法有效组织仓库，亟需一种自动构建层次分类体系的方法。

Method: 结合LLM全局知识与真实仓库分布，通过设计师代理和分类器代理的自我纠正迭代过程，自动提出并修正划分维度，构建层次分类体系。

Result: 在54,387个仓库上，ATLAS的TQF达到83.13%，优于基线15个百分点，并在下游任务（替代发现P@1=85.71%，仓库检索最高）中表现优异。

Conclusion: ATLAS是首个能自动构建高质量、实用层次分类体系的方法，揭示了扁平方法无法观察到的生态系统结构趋势。

中文摘要：GitHub上的开源生态系统缺乏系统性的软件仓库层次分类体系。GitHub Topics作为主要的组织机制，是扁平化、不一致的，并且仅覆盖了67%的项目。我们提出了ATLAS，这是第一个自动构建软件仓库层次分类体系并端到端对项目进行分类的框架。通过将LLM的全局知识与真实的仓库分布相结合，ATLAS提出了有意义的划分维度，并迭代地修正那些无法容纳实际项目的维度。一个设计师代理提出划分维度，而分类器代理分配仓库；自我纠正的细化循环利用分类失败，通过升级策略驱动维度修订。我们在54,387个GitHub仓库上评估了ATLAS，对比了涵盖四种范式、两个下游任务和三个模型家族的六个基线。在一个分层的2,001个仓库基准测试中，ATLAS达到了83.13%的分类质量F1分数（TQF），比最佳基线高出15个百分点（在全5.4万语料库上，近似TQF为73.0%，这一差距是由路径粒度在较长路径上的全有或全无评分导致的，而非分类准确率较低）。它是唯一同时实现高结构质量和高度实用性的方法。在下游任务中，ATLAS实现了P@1=85.71%的替代发现，甚至超过了人工策划的列表（62.34%），并在仓库检索中达到了最高的P@1。该分类体系进一步揭示了从扁平标签或相似性方法难以获得的生态系统结构趋势：从库到AI/ML应用的转变（现在占新社区采纳项目的61%）只有通过基于层次的类型分类才变得可见。交互式分类探索器可在https://atlas-taxonomy.netlify.app/上获取。

Original abstract: The open-source ecosystem on GitHub lacks a systematic hierarchical taxonomy of software repositories. GitHub Topics, the dominant organizational mechanism, is flat, inconsistent, and covers only 67% of projects. We present ATLAS, the first framework that automatically constructs a hierarchical taxonomy for software repositories and classifies projects into it end-to-end. By combining LLM global knowledge with real repository distributions, ATLAS proposes meaningful splitting dimensions and iteratively corrects those that fail to accommodate real projects. A Designer Agent proposes splitting dimensions while a Classifier Agent assigns repositories; a self-corrective refinement loop uses classification failures to drive dimension revision through escalating strategies. We evaluate ATLAS on 54,387 GitHub repositories against six baselines spanning four paradigms, two downstream tasks, and three model families. On a stratified 2,001-repository benchmark, ATLAS achieves a Taxonomy Quality F-score (TQF) of 83.13%, outperforming the best baseline by 15 percentage points (on the full 54k corpus the approximate TQF is 73.0%, a gap driven by Path Granularity's all-or-nothing scoring on longer paths rather than lower classification accuracy). It is the only method to simultaneously achieve high structural quality and high practical applicability. On downstream tasks, ATLAS enables alternative discovery with P@1 = 85.71%, surpassing even human-curated lists (62.34%), and achieves the highest P@1 for repository retrieval. The taxonomy further reveals structural ecosystem trends that are difficult to obtain from flat tags or similarity methods: the shift from libraries to AI/ML applications (now 61% of newly community-adopted projects) becomes visible only through hierarchical, type-based categorization. An interactive taxonomy explorer is available at https://atlas-taxonomy.netlify.app/

</details>


### [7] [The Alignment Problem in Constrained Code Generation](http://arxiv.org/abs/2606.21619v1)
*Matteo Biagiola,Jahrim Gabriele Cesario,Luca Di Grazia,George Zakhour,Guido Salvaneschi*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 约束解码中的约束器不完整性会严重损害代码生成的功能正确性，甚至使无约束解码表现更好。


<details>
  <summary>Details</summary>
Motivation: 先前工作忽略了约束解码中约束器与语言模型及目标语言之间的对齐问题，不完整性导致的偏差可能损害代码生成性能。

Method: 使用7种语言模型、2种目标语言和2种约束器，在解码中强制类型和语法，比较有约束与无约束解码，并研究不同不完整性水平的影响。

Result: 在三个基准测试上，当约束器不完整时，无约束解码的功能正确性显著更高；不完整性使功能正确性降低高达97%。

Conclusion: 使社区意识到约束解码中错位的负面影响，并为设计有益于代码生成系统的约束器提供定量见解。

中文摘要：大型语言模型（LLMs）在代码生成方面展现出了强大的能力，但其输出经常包含导致编译失败的语法或类型错误。约束解码被提出作为一种通过构造来缓解编译错误的解决方案，并作为副产品提高了功能正确性。然而，先前的工作忽略了约束解码的一个关键方面：约束器（例如类型）、语言模型与目标规范语言（例如TypeScript）之间的对齐。错位是由约束器不完整（拒绝属于目标的程序）或不健全（允许不属于目标的程序）引起的。不完整性造成的偏差扭曲了语言模型分布，并可能对代码生成产生不利影响。我们使用七个语言模型、两个目标语言、两个约束器评估了这一假设，在解码过程中强制执行类型和语法，并研究语言模型如何应对不同水平的不完整性。在三个基准测试上，当约束器不完整时，无约束解码在功能正确性方面显著优于约束解码。不完整性将模型推入程序空间的低概率区域，导致生成频繁超时，并将功能正确性降低高达97%。这些贡献使社区意识到约束解码中错位的负面影响，并提供了关于如何设计对具有形式保证的代码生成系统有益的约束器的定量见解。

Original abstract: Large Language Models (LLMs) have demonstrated strong capabilities in code generation, but their outputs frequently contain syntax or type errors that result in compilation failures. Constrained decoding has been proposed as a solution to mitigate compilation errors by construction, improving functional correctness as a byproduct. However, previous works overlook a critical aspect of constrained decoding: the alignment between constrainer (e.g., types), language model and the target specification language (e.g., TypeScript). Misalignment is caused by the constrainer being incomplete--rejecting programs that belong to the target--or unsound--allowing programs that are not part of the target. The bias created by incompleteness distorts the language model distribution, and can be detrimental for code generation.
  We evaluate this hypothesis using seven language models, two target languages, two constrainers, enforcing types and syntax during decoding, and we study how language models react to varying levels of incompleteness. On three benchmarks, when the constrainer is incomplete, unconstrained decoding significantly outperforms constrained decoding in terms of functional correctness. Incompleteness pushes the model into low-probability regions of the program space, causing the generation to frequently time out, and reducing functional correctness by up to 97%.
  These contributions make the community aware of the negative effects of misalignment in constrained decoding, and provide quantitative insights on how to design constrainers that are beneficial for code generation systems with formal guarantees.

</details>


### [8] [Assessing Language Models for Salient Class Identification](http://arxiv.org/abs/2606.21629v1)
*Bo Xiong,Chaoran Cai,Kaipeng Xiong,Chong Wang,Peng Liang*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 研究语言模型在代码提交中直接识别显著类的可行性，发现轻量开源SLM（如Qwen3.5-9B）在少样本提示下性能可媲美大型闭源LLM，为工业应用提供低成本隐私友好方案。


<details>
  <summary>Details</summary>
Motivation: 现有代码审查中识别显著类的方法依赖复杂程序分析（如AST解析、特征工程），成本高且不通用，需要更简单直接的方法。

Method: 构建ApacheJavaCM数据集，使用GPT-5.4、DeepSeek-V3.2和Qwen3.5-9B等模型，结合零样本、少样本和思维链提示，直接从提交文本识别显著类。

Result: 语言模型（特别是Qwen3.5-9B少样本）显著优于现有基线，性能稳定，且9B参数SLM与大型闭源LLM GPT-5.4性能相当。

Conclusion: 轻量级可本地部署的SLM足以胜任工业级显著类识别任务，可降低成本和隐私风险。

中文摘要：代码审查要求审查者理解代码变更的核心意图，当一次提交修改多个类时，这变得困难。在此类提交中，一个或多个主要被修改的类（称为显著类）可能诱发对其他类的修改。准确识别显著类为审查者提供了导航代码变更的有效入口点，并有助于程序理解。现有的最先进方法依赖于复杂的程序分析过程，包括抽象语法树（AST）解析、类关系提取、手工特征工程或依赖图构建。为此，我们研究语言模型（LM）是否能够直接从提交中识别显著类，无需特征工程、图构建或训练。我们首先构建了一个新数据集 ApacheJavaCM，源自 ApacheCM 数据集，包含 7,911 次提交和 25,914 个标记类。在此数据集上，我们系统评估了 LM 是否能够直接从提交中识别显著类，并与最强的可复现最先进（SOTA）基线进行比较。评估涵盖了两个大型语言模型（LLM）：GPT-5.4 和 DeepSeek-V3.2，一个小型语言模型（SLM）：Qwen3.5-9B，以及三种提示策略：零样本、少样本和思维链。LM 显著优于基线，并且在提交特征和所选 LM 上保持稳定。我们还发现，对于显著类识别任务，一个 9B 参数的开源 SLM Qwen3.5-9B 在少样本提示下实现了与更大的闭源 LLM GPT-5.4 相当的性能。这些结果表明，轻量级、可本地部署的 SLM 足以满足工业显著类识别任务，并且可以降低与依赖闭源 LLM 相关的成本和隐私障碍。

Original abstract: Code review requires reviewers to understand the core intent of code changes, which becomes difficult when a commit modifies multiple classes. In such commits, one or more primarily modified classes, referred to as salient classes, may induce modifications in other classes. Accurate identification of salient classes offers reviewers an effective entry point to navigate code changes and facilitates program comprehension. Existing state-of-the-art approaches rely on complex program-analysis procedures, including Abstract Syntax Tree (AST) parsing, class relation extraction, handcrafted feature engineering, or dependency graph construction. To this end, we study whether language models (LMs) can identify salient classes directly from commits without feature engineering, graph construction, or training. We first construct a new dataset ApacheJavaCM, derived from the ApacheCM dataset, containing 7,911 commits and 25,914 labeled classes. On this dataset, we systematically evaluate whether LMs can identify salient classes directly from commits and compare with the strongest reproducible state-of-the-art (SOTA) baseline. The evaluation covers two large language models (LLMs), GPT-5.4 and DeepSeek-V3.2, one small language model (SLM), Qwen3.5-9B, and three prompting strategies: zero-shot, few-shot, and chain-of-thought. The LMs substantially outperform the baseline while remaining stable across commit characteristics and selected LMs. We also found that, for salient class identification tasks, a 9B-parameter open-source SLM, Qwen3.5-9B, under few-shot prompting, achieves performance comparable to that of a much larger closed-source LLM, GPT-5.4. These results suggest that lightweight, locally deployable SLMs are sufficient for industrial salient class identification tasks and can reduce both cost and privacy barriers associated with relying on closed-source LLMs.

</details>


### [9] [ConcernBERT: Learning Responsibilities Using Class Membership](http://arxiv.org/abs/2606.21647v1)
*J. Lefever,J. Xu,Y. Cai,R. Kazman,E. Pisch*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 提出ConcernBERT模型，利用BERT和三元组损失学习实体嵌入，以识别内聚的程序实体组，并在大规模数据集上取得显著性能提升。


<details>
  <summary>Details</summary>
Motivation: 尽管关注点分离、高内聚等原则广为人知，但实际应用依赖直觉，缺乏可操作方法，现有识别内聚组的技术仍不可靠。

Method: 提出ConcernBERT，基于BERT的实体级嵌入模型，使用三元组损失优化方法/属性嵌入空间中的相对位置，并利用类成员上下文学习职责和关注点；同时构建大规模可复现数据集。

Result: 在合并多个类的方法并恢复原始类成员关系的任务上，ConcernBERT性能显著优于现有模型。

Conclusion: ConcernBERT有效编码关注点级语义，为架构恢复、重构等下游任务奠定基础。

中文摘要：关注点分离、高内聚和单一职责原则是软件设计中最著名的原则之一。然而，它们在实际应用中往往停留在哲学层面而非可操作层面，严重依赖开发者的直觉和经验。许多软件任务，如上帝类分解、提取类重构和内聚度量，都依赖于识别程序实体内聚组的技术，即这些实体共同履行一个共同职责。然而，可靠地识别这样的组仍然是一个挑战。在本文中，我们提出了ConcernBERT，一种基于BERT的嵌入模型，在实体级别进行训练，使用三元组损失直接优化方法和属性在嵌入空间中的相对位置，并利用类成员上下文来学习职责和关注点。我们还贡献了一个大规模的可复现数据集用于训练和评估。我们的数据集涵盖来自超过6000个仓库的超过200万个Java文件。为了评估ConcernBERT，我们将来自两个或多个类的方法合并成未标记的组，并测试模型恢复原始类成员关系的能力。ConcernBERT取得了比现有模型显著更高的性能，证明了其在编码关注点级语义方面的有效性，并为架构恢复、提取类重构和内聚度量等下游任务奠定了坚实的基础。

Original abstract: The principles of separation of concerns, high cohesion, and single responsibility are among the most well-known in software design. However, their application often remains philosophical rather than actionable, relying heavily on developers' intuition and experience. Many software tasks, such as god class decomposition, extract class refactoring, and cohesion measurement, depend on techniques for identifying cohesive groups of program entities, that is, entities that collectively fulfill a common responsibility. Yet reliably identifying such groups remains a challenge. In this paper, we propose ConcernBERT, a BERT-based embedding model trained at the entity level that uses triplet loss to directly optimize the relative positioning of methods and attributes in the embedding space, and uses class-membership context to learn responsibilities and concerns. We also contribute a large-scale replication dataset for training and evaluation. Our dataset spans over two million Java files across more than six thousand repositories. To evaluate ConcernBERT, we merge methods from two or more classes into unlabeled groups and test the model's ability to recover the original class memberships. ConcernBERT achieves significantly higher performance than existing models, demonstrating its effectiveness at encoding concern-level semantics and establishing a strong foundation for downstream tasks such as architecture recovery, extract class refactoring, and cohesion measurement.

</details>


### [10] [Humor in Software Testing Education](http://arxiv.org/abs/2606.21682v1)
*Isabella Graßl,Benoit Baudry*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 在软件测试课程中引入幽默元素能显著提升学生的情感投入、归属感和创造性思维，尤其对女生效果更佳。


<details>
  <summary>Details</summary>
Motivation: 软件测试常被学生视为单调枯燥，影响情感投入，而幽默在其他领域能提升参与度，但在测试教育中研究不足。

Method: 在加拿大和德国大学的入门软件测试课程中引入幽默元素，采用混合方法研究（问卷调查和定性反馈）评估效果。

Result: 幽默显著提升学生参与度、减少单调感，增强归属感和创造性思维，对女生效果更强。

Conclusion: 幽默是一种低门槛、有效的教学方法，能创造更包容的学习环境，尤其有利于女生。

中文摘要：软件测试常被认为是单调乏味的，这可能会对学生在测试中的情感投入产生负面影响。虽然先前的研究表明幽默可以在专业软件开发环境中提高参与度，但我们对幽默在软件测试教育中的作用知之甚少。本文探讨了软件测试作业中的幽默元素如何影响学生的情感投入、归属感和创造性思维。我们在加拿大和德国的大学入门软件测试课程中引入了幽默，并对学生进行了混合方法研究。我们的结果表明，幽默对学生的软件测试体验产生了强烈的积极影响。学生们认为测试更有趣、更不单调，在课堂上感到更舒适和被接受，并且报告了对测试任务的创造性思维有所增加。这些影响对女学生尤为强烈，尤其是在归属感方面。我们的发现表明，幽默代表了一种低门槛的教学方法，对学生有益，并有可能创造更受欢迎的学习环境。

Original abstract: Software testing is often perceived as monotonous, which can negatively influence students' emotional engagement with testing. While prior work suggests that humor can increase engagement in professional software development contexts, we know little about humor's effect in software testing education. This paper explores how humorous elements in software testing assignments affect students' emotional engagement, sense of belonging, and creative thinking. We introduced humor in introductory software testing courses at universities in Canada and Germany, and conducted a mixed-methods study with students. Our results show that humor had a strong positive influence on students' experiences of software testing. Students perceived testing as more engaging and less monotonous, felt more comfortable and accepted in class, and reported increased creative thinking about testing tasks. These effects were particularly strong for female students, especially with respect to sense of belonging. Our findings suggest that humor represents a low-threshold pedagogical approach which is beneficial for students, and has the potential to create a more welcoming learning environment.

</details>


### [11] [Towards Imputation of Pre-Trained Language Model Metadata using Semantic Fingerprinting](http://arxiv.org/abs/2606.21787v1)
*Adekunle Ajibode,Oussama Ben Sghaier,Keheliya Gallaba,Bram Adams,Ahmed E. Hassan*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 提出SemFin方法，利用HF配置文件和仓库标签自动填补PTLM缺失元数据并重构谱系链，显著优于基线。


<details>
  <summary>Details</summary>
Motivation: PTLM仓库因元数据缺失导致来源不可靠，缺乏类似软件依赖图的谱系信息。

Method: SemFin结合HF配置文件和仓库标签，自动填补缺失元数据字段，重构模型谱系链。

Result: 在317,133个PTLM上，SemFin优于传播方法，准确率提升31.4%和26.6%，为16.6%孤立模型填补元数据；应用于167,089个模型后，重用方法链扩展75.9%，许可证链扩展53.6%，发现86种新模式，不兼容许可证比例仅增2%。

Conclusion: 自动结构信号可支持AIBOM构建，将元数据从手动声明转为从模型工件推断。

中文摘要：托管在Hugging Face等平台上的预训练语言模型（PTLM）形成了类似于软件依赖图的复杂谱系结构。然而，与传统软件生态系统不同，PTLM仓库由于缺少元数据（如许可证、重用方法、管道标签、模型类型和训练库）而往往缺乏可靠来源。为了解决这一差距，我们引入了语义指纹（SemFin），这是一种轻量级方法，结合Hugging Face（HF）配置文件与模型仓库标签，自动填补缺失的模型元数据字段并重构模型谱系链。我们在包含317,133个PTLM的大规模数据集上评估了SemFin。结果表明，配置文件通常编码了实例化和重用模型所需的技术要求，使其能够作为模型重用的结构蓝图，尤其是对于基于Transformer的架构。通过结合这些配置文件和模型仓库标签，SemFin显著优于现有的基于传播的填补方法，与Graph Avg和Hub Avg基线相比，预测准确率分别提高了31.4%和26.6%。重要的是，SemFin还为16.6%的孤立模型填补了元数据，而基于传播的方法在这些模型上失败。将SemFin应用于为167,089个未标记模型填补缺失的重用方法和许可证元数据，发现可追踪的重用方法链扩展了75.9%，许可证谱系链扩展了53.6%，揭示了86个以前不可见的重用方法模式，而不兼容许可证模式的比例仅从34.8%增加到36.8%。这些发现表明，自动推导的结构信号如何支持AI物料清单（AIBOM）的自动构建，帮助将元数据从容易出错的手动声明转变为直接从模型工件中推断的信息。

Original abstract: Pre-trained language models (PTLMs) hosted on platforms such as Hugging Face form complex lineage structures similar to software dependency graphs. However, unlike traditional software ecosystems, PTLM repositories often lack reliable provenance due to missing metadata, such as licenses, reuse methods, pipeline tags, model types, and training libraries. To address this gap, we introduce Semantic Fingerprinting (SemFin), a lightweight approach that combines Hugging Face (HF) configuration files with model repository tags to automatically impute missing model metadata fields and reconstruct model lineage chains. We evaluate SemFin on a large-scale dataset of 317,133 PTLMs. Our results show that configuration files typically encode the technical requirements necessary to instantiate and reuse models, enabling them to serve as a structural blueprint for model reuse, particularly for transformer-based architectures. By combining these configuration files with model repository tags, SemFin significantly outperforms the existing propagation-based imputation approaches, improving prediction accuracy by up to 31.4% and 26.6% compared to Graph Avg and Hub Avg baselines. Importantly, SemFin also imputes metadata for 16.6% of isolated models where propagation-based methods fail. Applying SemFin to impute missing reuse-method and license metadata for 167,089 unlabeled models reveals that traceable reuse method chains expand by 75.9% and license lineage chains by 53.6%, uncovering 86 previously invisible reuse method patterns, while the proportion of incompatible license patterns only increases from 34.8% to 36.8%. These findings demonstrate how automatically derived structural signals can support the automated construction of AI Bills of Materials (AIBOMs), helping transform metadata from an error-prone manual declaration into information inferred directly from model artifacts.

</details>


### [12] [Is Agent Code Less Maintainable Than Human Code?](http://arxiv.org/abs/2606.21804v1)
*Shaswat Patel,Betty Li Hou,Arun Purohit,Kai Xu,Jane Pan,He He,Valerie Chen*

Main category: cs.SE

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 代理代码在可维护性上不如人类代码，导致下游任务解决率下降，传统指标无法解释，而行为差异是关键。


<details>
  <summary>Details</summary>
Motivation: 研究代理代码在维护场景下的可维护性，因为现有评估只关注单任务解决，忽略了代理代码的长期影响。

Method: 提出CodeThread框架，从仓库级基准构建受控实验，比较代理和人类代码的维护效果，并使用回归分析探索影响因素。

Result: 代理代码导致任务解决率下降高达13.1%，传统可维护性指标无法解释，但代理代码在输入验证、错误处理等行为上有差异，且与下游代码大小和任务难度相关。

Conclusion: 应同时评估即时任务解决和代码可维护性，代理代码的行为差异是下游错误的重要来源。

中文摘要：可维护性是软件工程的一个核心维度，塑造了代码的编写、审查和随时间演化的方式。虽然编码代理在单问题任务上表现出色，但当未来代理在其基础上构建时，其代码的可维护性仍不清楚，可能导致下游效应的累积。我们研究了代理代码与人类代码在这些维护场景中的比较，提出了CodeThread框架，用于从仓库级编码基准中构建受控实验。将CodeThread应用于四个前沿编码代理和四个基准，我们发现代理在基于代理代码而非人类代码解决任务时效率较低，任务解决率下降高达13.1%。回归分析表明，许多传统的软件工程可维护性指标无法解释这一差异。相反，最清晰的信号是代理代码中更微妙的行为差异，例如对输入验证和错误处理的更改，以及下游代码大小和任务难度的差异。这些发现强调需要不仅通过即时任务解决来评估这些系统，还要通过代码可维护性，并指出了代理代码引入下游错误的潜在来源。

Original abstract: Maintainability is a core dimension of software engineering, shaping how code is written, reviewed, and developed over time. While coding agents have demonstrated strong performance on single-issue tasks, it remains unclear how maintainable their code is when future agents build on top of it, potentially leading to compounding downstream effects. We investigate how agent code compares to human code in these maintenance settings, presenting CodeThread, a framework to construct controlled experiments from repository-level coding benchmarks. Applying CodeThread to four frontier coding agents and four benchmarks, we find that agents are less effective at resolving tasks when building on agent code compared to human code, with task resolve rate drops of up to 13.1%. Regression analysis reveals that many traditional software engineering maintainability metrics do not explain this difference. Instead, the clearest signals are subtler behavioral differences in agent code, such as changes to input validation and error handling, along with differences in downstream code size and task difficulty. These findings highlight the need to evaluate these systems not only by immediate task resolution but also by code maintainability, and point to potential sources of downstream errors introduced by agent code.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [13] [BioInsight: Multi-Agent Orchestration for Interactive Biomedical Knowledge Discovery](http://arxiv.org/abs/2606.20997v1)
*Jieyi Wang,Bingxuan Li,Nanyi Jiang,Desong Meng,Zirui Fan,Yuxin Guo,Jiayu Liu,Kunlun Zhu,Eddie Yang,Xiusi Chen,Pan Lu,Bingxin Zhao*

Main category: cs.AI

Selection reason: 多智能体编排框架，可迁移至coding agent的协作设计

TL;DR: BioInsight是一个多智能体系统，将静态生物医学报告转化为交互式界面，提升证据探索和决策支持能力。


<details>
  <summary>Details</summary>
Motivation: 静态AI生成报告不足以支持用户检查证据、评估不确定性和完善假设，需要交互式证据中心界面。

Method: 通过多智能体系统，将证据检索与机制推理分离，使用确定性组件规范化引用，并将结构化证据转换为交互式界面。

Result: 在标准化生物医学问答、蛋白质功能推理和端到端证据合成任务上取得最佳性能。

Conclusion: 生物医学AI系统应转向保留来源的交互式证据产物，而非仅文本和静态报告。

中文摘要：生物医学研究者越来越多地使用AI生成的分析和报告来解释蛋白质水平的信号，但静态输出通常不足以支持研究决策，因为用户需要检查证据、评估不确定性、比较机制并完善假设。我们提出BioInsight，一个多智能体系统，从静态的生物医学报告生成转向以证据为中心的交互式界面生成。给定疾病名称、蛋白质关联表和可选的队列元数据，BioInsight通过类型化的中间产物组织疾病特异性证据，包括排序通路、文献证据包、蛋白质级推理笔记、引用支持的报告、仪表盘架构和渲染的交互界面。该系统将证据检索与机制推理分离，通过确定性组件规范化引用，并将报告中使用的相同结构化证据转换为交互式界面。我们在标准生物医学问答、具有挑战性的蛋白质功能推理以及端到端生物医学证据合成上评估BioInsight。结果表明BioInsight取得了最佳效果，并建议生物医学AI系统应超越纯文本和静态报告，转向保留来源的交互式证据产物。

Original abstract: Biomedical researchers increasingly use AI-generated analyses and reports to interpret protein-level signals, but static outputs are often insufficient for research decision-making, where users need to inspect evidence, assess uncertainty, compare mechanisms, and refine hypotheses. We present \textsc{BioInsight}, a multi-agent system that moves from static biomedical report generation to interactive evidence-centered interactive interface generation. Given a disease name, a protein association table, and optional cohort metadata, BioInsight organizes disease-specific evidence through typed intermediate artifacts, including ranked pathways, literature evidence packets, protein-level reasoning notes, citation-grounded reports, dashboard schemas, and rendered interactive interfaces. The system decomposes evidence retrieval from mechanistic reasoning, normalizes citations through deterministic components, and converts the same structured evidence used in the report into an interactive interface. We evaluate BioInsight on standardized biomedical QA, challenging protein-function reasoning, and end-to-end biomedical evidence synthesis. Results show that BioInsight achieves best, and suggest that biomedical AI systems should move beyond text-only and static reports toward provenance-preserving, interactive evidence artifacts.

</details>


### [14] [Building Agent Harnesses for Scientific Curation from Multimodal Sources](http://arxiv.org/abs/2606.21005v2)
*Sheng Zhang,Qin Liu,Renqian Luo,Shufang Xie,Reuben Tan,Sean Hayes,Gregory Bryman,Wendong Ge,Ruilian Zhang,Oluwaseun Egbelowo,Kelly Yee,Hoifung Poon*

Main category: cs.AI

Selection reason: 提出通用agent harness设计，包含工具使用、迭代评估、多模态证据，可迁移至代码优化agent

TL;DR: Beaver是一个代理框架，通过多模态证据工具、任务脚手架和迭代优化，显著提升了从科学论文中提取结构化信息的性能，在GRAS指标上达到81.0，超越前沿代理23个绝对点。


<details>
  <summary>Details</summary>
Motivation: 科学发现工作流需要从文献中结构化整理信息，但当前代理难以处理分散在多模态证据中的关键信息，且最终记录需要跨片段推理，因此需要新的方法。

Method: Beaver结合前沿代理、多模态证据工具、任务脚手架和工件导向的自动研究，将整理转化为分阶段、可审计的工作流，并支持迭代的评估-诊断-修改循环。

Result: Beaver在GRAS指标上达到81.0，比前沿代理提高23个绝对点；消融实验表明任务脚手架、多模态证据工具和来源追踪均有贡献，尤其在高价值属性上增益最大。

Conclusion: 对于从多模态科学论文中进行的科学整理，框架设计是决定代理性能的核心因素。

中文摘要：科学发现工作流通常依赖于从文献中进行的结构化整理。这对当前代理来说很困难，因为关键证据分散在长文本、密集表格和图表中，最终记录通常需要对多个证据片段进行推理，而非复制单个片段。我们研究来自多模态来源的科学整理，并引入了Beaver，一个从科学论文中提取结构化信息并保留证据来源的代理框架。Beaver结合了前沿代理与多模态证据工具、任务脚手架和基于工件的人工研究。这些组件将整理转化为一个分阶段、可审计的工作流，并支持迭代的评估-诊断-修改循环，其中持久运行工件暴露阶段局部故障并引导框架更新。实验表明，Beaver在金标准引用属性评分（GRAS）上达到81.0，这是一个属性级别衡量与金标准整理记录一致性的指标，比前沿代理高出超过23个绝对点。消融实验表明，任务脚手架、多模态证据工具和来源追踪各自对性能有显著贡献，而属性级别分析显示，在需要跨模态推理和归一化的高价值属性上增益最大。这些结果表明，对于从包含多模态证据的论文中进行科学整理，框架设计是代理性能的核心决定因素。

Original abstract: Scientific discovery workflows often depend on structured curation from the literature. This is difficult for current agents because the key evidence is scattered across long text, dense tables, and figures, and the final records often require reasoning across multiple evidence fragments rather than copying a single span. We study scientific curation from multimodal sources and introduce Beaver, an agent harness that extracts structured information from scientific papers while preserving provenance to the supporting evidence. Beaver combines a frontier agent with multimodal evidence tooling, task scaffolding, and artifact-grounded autoresearch. These components turn curation into a staged, auditable workflow and enable an iterative evaluate--diagnose--revise loop, where persistent run artifacts expose stage-localized failures and guide harness updates. Experiments show that Beaver reaches 81.0 on Gold-Referenced Attribute Score (GRAS), an attribute-level measure of agreement with gold curated records, outperforming frontier agents by over 23 absolute points. Ablations show that task scaffolding, multimodal evidence tooling, and provenance traces each contribute meaningfully to performance, while attribute-level analysis shows the largest gains on high-value attributes that require cross-modal reasoning and normalization. These results show that, for scientific curation from papers with multimodal evidence, harness design is a central determinant of agent performance.

</details>


### [15] [Agentic Time Machine as an Infrastructure for Future-Event Forecasting](http://arxiv.org/abs/2606.21013v1)
*Jingyi Chai,Bingyang Zheng,Xiangrui Liu,Hao Lu,Zihang Zhou,Tianchen Wang,Kemeng Zhang,Siheng Chen*

Main category: cs.AI

Selection reason: 多智能体未来事件预测框架，planner-solver-aggregator架构可迁移至coding agent

TL;DR: 提出Agentic Time Machine (TM)基础设施，通过过滤截止后内容重建历史网络状态，用于高效评估预测代理；并设计多代理框架在多个基准上取得最优结果。


<details>
  <summary>Details</summary>
Motivation: 现有预测代理评估存在实时基准反馈慢和回顾性重放环境保真度低的问题，需要一种既能快速评估又保持环境真实性的方法。

Method: 构建Agentic Time Machine (TM)基础设施，通过过滤截止后内容近似重建过去网络状态；提出规划者-求解者-聚合者多代理框架，分解问题、并行收集证据并组合预测。

Result: TM下离线分数与实时FutureX分数强相关；在FutureX-Past和Polymarket上取得最高分；在FutureX实时排行榜上连续四周最佳平均排名，最终排名第一。

Conclusion: TM验证了作为快速可靠沙箱的有效性，所提框架在多个预测基准上达到最优，证明了方法的实用性。

中文摘要：预测未来事件对大型语言模型（LLM）代理来说是一个关键挑战，涵盖从选举、货币政策到金融市场等各个领域。然而，评估这一任务的进展面临效率与环境保真度之间的根本权衡。虽然实时评估基准存在固有的慢反馈循环，但现有的回顾性重放通常将代理限制在静态的、预先冻结的数据库中，牺牲了实际部署的环境真实性。为解决这个问题，我们引入了Agentic Time Machine (TM)，这是一种通过过滤截止后内容来近似重建任何选定过去时间的网络状态的基础设施。借助这一评估基础设施，我们进一步提出了一个规划者-求解者-聚合者多代理框架，该框架将每个问题分解为不同的分析角度，并行收集证据，并将结果组合成单个预测。实验表明，在TM下的离线分数与实时FutureX分数强相关，验证了TM为预测代理评估提供了一个快速且可靠的沙箱。在TM评估的FutureX-Past和Polymarket上，我们的框架在强封闭书、工具增强和自一致性基线中取得了最高分。在官方FutureX实时排行榜上，我们的系统在连续四周内取得了最佳平均排名，包括5月第一周的第一名。截至6月17日，它在FutureX官方八周总排行榜上也排名第一。

Original abstract: Forecasting future events is a critical challenge for large language model (LLM) agents, spanning domains from elections and monetary policy to financial markets. However, evaluating progress on this task presents a fundamental trade-off between efficiency and environment fidelity. While live evaluation benchmarks suffer from an inherently slow feedback loop, existing retrospective replays typically restrict agents to static, pre-frozen databases that sacrifice the environmental realism of actual deployments. To tackle this issue, we introduce Agentic Time Machine (TM), an infrastructure that approximately reconstructs the web state at any chosen past time by filtering post-cutoff content. Leveraging this evaluation infrastructure, we further propose a planner-solver-aggregator multi-agent framework that breaks each question into diverse analytical angles, gathers evidence in parallel, and combines the results into a single forecast. Experiments show that offline scores under TM correlate strongly with live FutureX scores, validating that TM offers a fast and reliable sandbox for forecasting-agent evaluation. On FutureX-Past and Polymarket evaluated under TM, our framework achieves the highest score among strong closed-book, tool-augmented, and self-consistency baselines. On the official FutureX live leaderboard, our system achieves the best average rank over four consecutive weeks, including 1st place in May Week 1. As of June 17, it also ranks 1st on FutureX's official eight-week overall leaderboard.

</details>


### [16] [Negative Knowledge as Failure-aware Shared Memory for AutoResearch](http://arxiv.org/abs/2606.21024v1)
*Hanchun Wang*

Main category: cs.AI

Selection reason: 直接提出负知识记忆层用于AutoResearch，可迁移至代码优化agent的失败经验复用

TL;DR: 提出负面知识记忆层，将失败尝试转化为结构化记录，使研究智能体在实验中明确采纳或拒绝，显著提升AI科研系统的性能与效率。


<details>
  <summary>Details</summary>
Motivation: AI辅助研究系统产生的失败尝试往往被丢弃，未能成为可利用的知识资产，限制了系统学习与改进能力。

Method: 构建负面知识记忆层，包括策展智能体将失败尝试转化为有界类型化记录存入共享库，以及下游研究智能体在提出实验前主动选择采纳或拒绝这些记录。

Result: 在ScienceAgentBench和PDE科学问题中，负面知识层相比AutoResearch基线使用更少令牌且表现更优，能解决基线无法完成的任务，并实现跨问题迁移。

Conclusion: 结构化负面知识应作为持久知识资产被明确维护，与正面发现共同构成科学记忆的集体基础设施。

中文摘要：人工智能辅助研究系统生成了许多失败的尝试，但这些失败很少成为持久的、共享的知识资产。我们提出了一种负面知识记忆层：一个策展智能体将每次失败的尝试转换为共享库中的一条有界、类型化的记录，而下游的研究智能体在提出下一次实验之前明确采纳或拒绝这些记录。我们在两种设置中评估了这一层：ScienceAgentBench上的同任务重试和两个非线性数学物理PDE问题上的跨任务科学研究。负面知识层在使用更少令牌的同时优于vanilla AutoResearch基线；拥有负面知识库的智能体解决了所有基线在PDE系统研究中无法解决的新任务。我们还表明，之前的负面知识库可以迁移并增强不同PDE问题上的AutoResearch。这些结果表明，结构化的负面知识是一种知识资产，应当被明确地维护，超越记忆压缩或调试辅助工具，与正面发现一起，作为科学记忆的集体基础设施，用于更广泛的人工智能参与的科学研究中。代码可在https://github.com/hch-wang/Negative_Knowledge获取。

Original abstract: AI-assisted research systems generate many failed attempts, but those failures rarely become a durable, shared knowledge asset. We propose a negative knowledge memory layer: a curator agent converts each failed attempt into a bounded, typed record in a shared bank, and a downstream research agent explicitly adopts or rejects those records before proposing its next experiment. We evaluate this layer in two settings: same-task retry on ScienceAgentBench and cross-task scientific research on two nonlinear math-physics PDE problems. The negative knowledge layer outperforms vanilla AutoResearch baselines while using fewer tokens; agents with the negative knowledge bank solve new tasks that all baselines fail to solve in PDE systems research. We also show that the previous negative knowledge bank can transfer and enhance AutoResearch on different PDE problems. These results suggest that structured negative knowledge is a knowledge asset that should be explicitly maintained in broader AI-engaged scientific research beyond a memory-compression or debugging aid, alongside positive findings, as a collective infrastructure for scientific memory. Code is available at https://github.com/hch-wang/Negative_Knowledge.

</details>


### [17] [Answer Engineering: Local Trajectory Editing for Protocol-Constrained Decision Making in Large Language Models](http://arxiv.org/abs/2606.21121v1)
*Victor Lavrenko,Anastasiia Molodnitskaia*

Main category: cs.AI

Selection reason: 提出局部轨迹编辑的运行时控制方法，可迁移到coding agent的合规性约束。

TL;DR: 提出Answer Engineering方法，通过局部规则引导的推理轨迹编辑提升大语言模型在临床协议中的合规性，在SSNHL基准上显著提高准确率。


<details>
  <summary>Details</summary>
Motivation: 大语言模型在程序合规性要求高的领域（如临床诊断）常给出自信但无效的答案，需要提升其协议依从性而不进行模型修改或全局搜索。

Method: Answer Engineering是一种确定性运行时和创作层，在标准自回归生成过程中，对可见推理轨迹应用局部规则引导的干预，无需重新训练、修改模型权重或全局搜索。

Result: 在SSNHL基准上，局部轨迹编辑将SSNHL合规性从25.1%提升至83.5%，传导性病例依从性从1.6%提升至77.9%，平衡准确率从42.0%提升至80.7%。

Conclusion: 通过可审计的运行时控制推理轨迹可提高协议依从性，同时指出规则覆盖、触发可靠性和诊断优先动态的限制。

中文摘要：大型语言模型在程序合规性至关重要的领域中可能会产生自信但协议无效的答案。本文提出了Answer Engineering，一个确定性的运行时和创作层，它在标准自回归生成过程中，在不重新训练、修改模型权重或执行全局搜索的情况下，对可见推理轨迹应用局部规则导向的干预。该方法在一个用于突发性感觉神经性听力损失（SSNHL）的受控临床基准上进行评估，该疾病的正确管理取决于对症状时间、Weber/Rinne音叉检查结果和耳镜检查结果的协议一致性解释。在该基准中，逐步推理转移了错误而非消除：SSNHL的合规结果从无引导生成下的54.5%下降到25.1%，而传导性对比条件下的接受率从1.6%上升到58.9%。局部轨迹编辑将SSNHL合规性提高到83.5%，传导性病例依从性提高到77.9%，平衡准确率从仅推理生成的42.0%提高到80.7%。结果支持一个系统级的观点，即通过可审计的推理轨迹运行时控制可以提高协议依从性，同时也指出了由规则覆盖范围、触发可靠性和持久的诊断优先生成动态造成的限制。

Original abstract: Large language models can produce confident but protocol-invalid answers in domains where procedural compliance is critical. This paper presents Answer Engineering, a deterministic runtime and authoring layer that applies localized rule-guided interventions to the visible reasoning trajectory during standard autoregressive generation, without retraining, modifying model weights, or performing global search. The method is evaluated on a controlled clinical benchmark for sudden sensorineural hearing loss (SSNHL), where correct management depends on protocol-consistent interpretation of symptom timing, Weber/Rinne tuning-fork findings, and otoscopic findings. In the benchmark, step-by-step reasoning shifted rather than eliminated errors: compliant outcomes for SSNHL decreased from 54.5% under unguided generation to 25.1%, while acceptance on the conductive contrast condition increased from 1.6% to 58.9%. Local trajectory editing increased SSNHL compliance to 83.5% and conductive-case adherence to 77.9%, raising balanced accuracy from 42.0% under reasoning-only generation to 80.7%. The results support a systems-level view in which protocol adherence can be improved through auditable runtime control of reasoning trajectories, while also identifying limitations caused by rule coverage, trigger reliability, and persistent diagnosis-first generation dynamics.

</details>


### [18] [PulseCX: Breaking the Closed-World Assumption in Real-Time CX](http://arxiv.org/abs/2606.21124v1)
*Rajat Agarwal,Suvidha Tripathi,Shubham Sharma*

Main category: cs.AI

Selection reason: 提出解耦知识获取与消费的CX agent框架，含分层意图门控与记忆，通用agent架构。

TL;DR: PulseCX通过解耦知识获取与消费、采用衰减感知时态知识图谱和层次化意图门控，实现了动态环境下低延迟、高意图解析率和客户满意度的对话AI。


<details>
  <summary>Details</summary>
Motivation: 现有对话AI受封闭世界约束，无法应对高速外部变化，而临时网络搜索带来延迟和上下文污染，亟需一种高效的知识管理方法。

Method: 提出PulseCX框架，采用异步代理将信号线性化为衰减感知时态知识图谱（DA-TKG），通过强化-衰减动力学管理信息生命周期，并结合层次化意图门控消除同步搜索瓶颈。

Result: PulseCX实现<10ms开销，在动态环境中显著提升意图解析率（IRR）和客户满意度（s-CSAT）。

Conclusion: PulseCX通过结构优先和解耦策略，有效解决了对话AI的封闭世界问题，为动态CX场景提供了高效的知识管理方案。

中文摘要：客户体验（CX）中的对话式AI代理通常受限于封闭世界约束，忽略了诸如病毒式趋势或服务中断等高速外部变化。临时性的网络搜索试图弥补这一差距，但往往带来高昂的延迟和上下文污染。我们引入了PulseCX，一个将知识获取与消费解耦的框架。采用结构优先的范式，PulseCX使用异步代理将信号线性化为一个由强化-衰减动力学控制的衰减感知时态知识图谱（DA-TKG），以主动管理信息生命周期。通过将这种自进化记忆与层次化意图门控相结合，PulseCX消除了同步搜索瓶颈（<10ms开销），并在动态环境中显著提高了意图解析率（IRR）和客户满意度（s-CSAT）。

Original abstract: Conversational AI agents in Customer Experience (CX) typically suffer from a Closed-World Constraint, ignoring high-velocity external shifts like viral trends or outages. Ad-hoc web search attempts to bridge this gap but often introduce prohibitive latency and context poisoning. We introduce PulseCX, a framework that decouples knowledge acquisition from consumption. Adopting a structure-first paradigm, PulseCX employs an asynchronous agent to linearize signals into a Decay-Aware Temporal Knowledge Graph (DA-TKG) governed by reinforcement--decay dynamics to actively manage information lifecycles. By coupling this self-evolving memory with hierarchical intent gating, PulseCX removes synchronous search bottlenecks (<10ms overhead) and drives significant gains in Intent Resolution (IRR) and Customer Satisfaction (s-CSAT) in dynamic environments.

</details>


### [19] [Trip+: Benchmarking Agents in Personalized Interactive Travel Planning](http://arxiv.org/abs/2606.21169v1)
*Junle Chen,Wei Chen,Yehong Xu,Zhengjun Huang,Yuqian Wu,Zhoujin Tian,Kai Wang,Lei Wang,Xiaofang Zhou*

Main category: cs.AI

Selection reason: 交互式旅行规划agent基准，涉及多轮个性化与重规划，通用agent评测与规划。

TL;DR: Trip+是一个用于评估语言模型在旅行规划中整体能力的基准，发现现有模型倾向于生成可行但不符合用户偏好的疲劳行程。


<details>
  <summary>Details</summary>
Motivation: 现有基准测试在评估旅行规划时往往孤立地考虑可行性、个性化或交互性，缺乏对端到端旅行体验的综合评估。

Method: 构建Trip+基准，包含旅行者画像、动态交互和分钟级行程规划，并利用基于LLM的模拟器评估主观体验（如疲劳度）。

Result: 评估了18个语言模型，发现所有模型在体验质量上存在一致差距，倾向于生成技术上可行但令用户疲惫且偏离偏好的行程。

Conclusion: 当前语言模型在复杂旅行规划中未能充分平衡可行性与用户体验，需要改进以生成更符合个性化偏好的舒适行程。

中文摘要：交互式旅行规划已成为语言模型的一个流行用例。智能体被部署来管理多轮交互中不断变化的偏好和意外中断。这种设置要求模型做出复杂的、基于用户画像的规划决策。然而，现有的基准测试通常只在相对孤立的环境中评估可行性、个性化或交互性。因此，我们引入了Trip+来衡量智能体整体规划旅行的能力。在Trip+中，给定旅行者画像和动态交互，智能体必须生成并修订分钟级的行程安排。通过基于LLM的模拟器评估端到端的旅行者体验，从而能够评估疲劳等主观指标。我们的场景范围从简单的请求处理到复杂的环境驱动的重新规划。我们评估了18个语言模型，并发现体验质量上存在一致的差距。模型偏向于技术上可行但令人疲惫的行程安排，这些行程与画像中的旅行者偏好严重偏离。

Original abstract: Interactive travel planning has become a popular use case for language models. Agents are deployed to manage evolving preferences and unexpected disruptions over multiple turns. Such settings require models to make complex, profile-conditioned planning decisions. However, existing benchmarks often evaluate feasibility, personalization, or interaction in relatively isolated settings. We therefore introduce Trip+ to measure the ability of agents to plan travel holistically. In Trip+, given traveler profiles and dynamic interactions, agents must generate and revise minute-level itineraries. End-to-end traveler experiences are evaluated via an LLM-based simulator, enabling the assessment of subjective metrics like fatigue. Our scenarios range from simple request resolutions to complex environment-driven replanning. We evaluate 18 LMs and find a consistent gap in experiential quality. Models favor technically feasible but exhausting itineraries that diverge sharply from profiled traveler preferences.

</details>


### [20] [ARCO: Adaptive Rubric with Co-Evolution for Multi-Step LLM-Based Agents](http://arxiv.org/abs/2606.21262v1)
*Zihang Tian,Jingsen Zhang,Rui Li,Xiaohe Bo,Yuanzi Li,Xu Chen*

Main category: cs.AI

Selection reason: 提出自适应rubric共进化框架，用于多步LLM agent的步骤级奖励分配，直接相关通用agent方法。

TL;DR: 提出ARCO框架，通过共享主干网络的生成头和评分头实现自适应rubric共演化，结合轨迹分解约束实现步骤级信用分配，在多跳QA任务上超越基线。


<details>
  <summary>Details</summary>
Motivation: 现有基于rubric的奖励方法在轨迹级别评分且评分器静态，无法解决步骤级信用分配问题。

Method: ARCO使用同规模模型μ，包含生成头（产生每步标准）和评分头（预测rubric条件步骤奖励），通过轨迹分解约束（步骤奖励和等于终端结果）实现无监督信用分配，并与策略π在策略数据上联合更新。

Result: 在HotpotQA、2WikiMultiHopQA和MuSiQue上，ARCO在每个设置中最佳EM均超过强基线，rubric步骤特定且鲁棒。

Conclusion: ARCO通过参数级共演化实现自适应rubric，有效改进步骤级信用分配和智能体行为诊断。

中文摘要：多步LLM智能体的强化学习通常依赖于表示成功与否的标量奖励，但无法解释轨迹的好坏。基于rubric的奖励通过自然语言标准提高了可解释性，但现有方法在轨迹级别评分，并将评分器冻结在闭源裁判之后，导致步骤级别的信用分配问题未解决且裁判本身是静态的。我们提出了ARCO（自适应rubric共演化），一个rubric框架，其中同规模模型μ共享一个主干网络和两个头：生成头产生每步标准，评分头预测基于rubric的步骤级别奖励。轨迹分解约束将步骤奖励之和与最终结果联系起来，实现了无步骤标签的信用分配，同时μ和策略π在策略数据上联合更新，使得rubric内容和评分函数在参数级别共演化。在HotpotQA、2WikiMultiHopQA和MuSiQue上使用两种开源主干网络，ARCO在每个设置中优于强结果、rubric和过程奖励基线，最佳EM均有提升，分析表明其rubric是步骤特定的、对设计选择鲁棒，并且有助于诊断智能体行为。代码和数据见https://github.com/zihangtian/ARCO。

Original abstract: Reinforcement learning for multi-step LLM agents often relies on scalar rewards that indicate success but cannot explain why a trajectory is good or bad. Rubric-based rewards improve interpretability through natural-language criteria, but existing methods score at the trajectory level and freeze the scorer behind a closed-source judge, leaving step-level credit assignment unresolved and the judge itself static. We propose ARCO (Adaptive Rubric CO-evolution), a rubric framework in which a same-scale model $μ$ shares a backbone with two heads: a generation head that produces per-step criteria, and a score head that predicts rubric-conditioned step-level rewards. A trajectory decomposition constraint ties the sum of step rewards to the terminal outcome, enabling credit assignment without step-level labels, while $μ$ and the policy $π$ are jointly updated on on-policy data so that the rubric content and the scoring function co-evolve at the parameter level. Across HotpotQA, 2WikiMultiHopQA, and MuSiQue with two open-source backbones, ARCO improves the best EM in every setting over strong outcome-, rubric-, and process-reward baselines, and analyses show that its rubrics are step-specific, robust to design choices, and useful for diagnosing agent behavior. Codes and data are available at https://github.com/zihangtian/ARCO.

</details>


### [21] [Social World Model for Lifelong Social Intelligence](http://arxiv.org/abs/2606.21315v1)
*Yu Luo*

Main category: cs.AI

Selection reason: 社会智能持续学习框架，含经验复用与benchmark，可迁移至agent长期学习。

TL;DR: 提出了社交世界模型，通过将社交互动分解为五个维度构建闭环学习框架，使小型开源模型能在ASCENT-Bench上持续学习并获得竞争力，实现零遗忘。


<details>
  <summary>Details</summary>
Motivation: 当前研究仅关注静态评估，缺乏对社交能力持续学习的支持，且存在互动轨迹无结构化表示、能力提升与保持孤立研究的问题。

Method: 提出社交世界模型，将社交互动分解为场景设置、观察、心智状态、行动、对话五个维度，构建闭环学习框架，包括经验收集、偏好信号转化、策略更新和持续学习，并提供可复用数据合成机制和终身学习基准。

Result: 在ASCENT-Bench上，交互训练的Qwen2.5-7B在所有五个核心指标上优于基线，完成率与Gemini 3 Flash持平，通过率超越，且实现零遗忘。

Conclusion: 该工作提供了可训练、可验证、可保持的端到端路径，证明小型开源模型能可持续获得竞争性社交协调能力。

中文摘要：社交智能是语言智能体的核心能力，然而当前研究主要关注静态能力评估，而非这些技能如何被持续塑造和积累。这一差距呼吁转向可持续学习范式。当前存在两个方法论痛点：社交互动轨迹缺乏统一的结构化表示以形成可迭代的学习信号，以及能力提升与保持通常被孤立研究，阻碍了对持续演化的评估。为弥补这一差距，我们提出了社交世界模型。我们将社交互动分解为五个维度（场景设置、观察、心智状态、行动和对话）以构建闭环学习框架。在此设置中，智能体收集互动经验，将其转化为偏好信号用于模型更新，并重新部署更新后的策略以持续学习。此外，我们提供可复用的数据合成机制和终身学习基准，将社交能力从“评估对象”转变为“可持续训练的对象”。在ASCENT-Bench上验证我们的框架，交互训练的Qwen2.5-7B模型在所有五个核心指标上均优于其基线。值得注意的是，它在完成率上与闭源的Gemini 3 Flash持平，在通过率上超越它，并在三个难度级别上实现了零遗忘。与以往仅报告静态比较或能力衰减的工作不同，这种端到端方法提供了可训练、可验证和可保持的路径，证明了小型开源模型可以可持续地获得具有竞争力的社交协调能力。

Original abstract: Social intelligence is a core competency for language agents, yet current research primarily focuses on static capability evaluation rather than how these skills are continuously shaped and accumulated. This gap calls for a shift toward sustainable learning paradigms. Currently, two methodological pain points exist: social interaction trajectories lack unified structured representations to form iterable learning signals, and capability improvement and retention are typically studied in isolation, hindering the assessment of continuous evolution.
  To bridge this gap, we propose the Social World Model. We decompose social interaction into five dimensions (scene setting, observation, mental state, action, and dialogue) to build a closed-loop learning framework. In this setup, agents collect interaction experiences, convert them into preference signals for model updating, and redeploy the updated policy for continued learning. Additionally, we provide a reusable data synthesis mechanism and a lifelong learning benchmark, transforming social capabilities from an "object of evaluation" into an "object of sustainable training".
  Validating our framework on the ASCENT-Bench, the interactively trained Qwen2.5-7B model outperforms its baseline across all five core metrics. Notably, it matches the closed-source Gemini 3 Flash in completion rate, exceeds it in pass rate, and achieves zero forgetting across three difficulty levels. Unlike prior works that merely report static comparisons or capability decay, this end-to-end approach provides a trainable, verifiable, and retainable pathway, demonstrating that small open-source models can sustainably acquire competitive social coordination capabilities.

</details>


### [22] [Calibration Is Not Control: Why LLM-Agent Oversight Needs Intervention](http://arxiv.org/abs/2606.21399v1)
*Chubin Zhang,Zhenglin Wan,Xingrui Yu,Jingxuan Wu,Qi Wen,Pengfei Zhou,Wangbo Zhao,Ivor Tsang*

Main category: cs.AI

Selection reason: LLM agent监督新框架，从校准转向干预，通用agent oversight方法。

TL;DR: LLM智能体的运行时监督应从标量风险预测转向行动条件价值估计，因为后者能更有效地提升干预效果。


<details>
  <summary>Details</summary>
Motivation: 现有标量风险预测框架瞄准了错误的控制对象，忽略了干预是否真正改善结果，导致相同风险估计下需要不同行动。

Method: 提出干预优势作为决策对象，并引入前缀分支协议来量化目标误差，通过行动条件控制优化监督决策。

Result: 在四个基准测试中，行动条件控制相比标量路由显著降低控制遗憾，尤其在强交互场景中（ALFWorld上遗憾从0.506降至0.110），而仅校准标量得分无法修复目标误差。

Conclusion: LLM智能体监督应转向行动条件价值估计，以更有效地控制代理行为。

中文摘要：大型语言模型（LLM）智能体的运行时监督通常被框架为标量风险预测：估计失败可能性、置信度或不确定性，一旦得分超过阈值就进行干预。我们认为这种框架瞄准了错误的控制对象。相关的问题不是如果智能体继续下去失败的可能性有多大，而是可用的干预措施是否能够改善结果。两个轨迹前缀可能具有相同的风险估计，但需要不同的行动，因为一个仍然可恢复而另一个不可恢复。我们将这种不匹配形式化为目标误差，并识别出干预优势——即干预而非继续的期望效用增益——作为监督的决策对象。为了衡量这种不匹配，我们引入了前缀分支，这是一种相同前缀的反事实协议，从相同的轨迹状态执行候选行动。在四个基准测试中，行动条件控制相比标量路由产生了取决于场景的增益。在校准分解中，重新校准相同的标量得分改善了预测指标，但控制遗憾保持不变，这表明仅靠校准无法修复目标误差。一个简单的前缀-only行动条件控制器在最强交互场景中将遗憾从0.506大幅降低到0.110（在ALFWorld上）。当干预较弱或标量路由已经保留了干预相关信息时，增益会缩小。这些结果表明，LLM智能体的监督应从标量风险评分转向行动条件价值估计。

Original abstract: Runtime oversight for LLM agents is commonly framed as scalar risk prediction: estimate failure likelihood, confidence, or uncertainty, then intervene once the score crosses a threshold. We argue that this framing targets the wrong object for control. The relevant question is not how likely the agent is to fail if it continues, but whether an available intervention would improve the outcome. Two trajectory prefixes can have the same risk estimate while requiring different actions, because one remains recoverable and the other does not. We formalize this mismatch as target error and identify intervention advantage, the expected utility gain from intervening rather than continuing, as the decision object for oversight. To measure this mismatch, we introduce prefix branching, a same-prefix counterfactual protocol that executes candidate actions from identical trajectory states. Across four benchmarks, action-conditioned control yields regime-dependent gains over scalar routing. In a calibration decomposition, recalibrating the same scalar score improves prediction metrics but leaves control regret unchanged, showing that calibration alone does not repair target error. A simple prefix-only action-conditioned controller substantially reduces regret in the strongest interactive regime, from 0.506 to 0.110 on ALFWorld. Gains shrink when interventions are weak or when scalar routing already preserves intervention-relevant information. These results suggest that LLM-agent oversight should move from calibrated risk scoring toward action-conditioned value estimation.

</details>


### [23] [Don't Blindly Trust It: How Unreliable Feedback Breaks Tool-Using LLM Agents](http://arxiv.org/abs/2606.21409v1)
*Chubin Zhang,Zhenglin Wan,Xingrui Yu,Pengfei Zhou,Wangbo Zhao,Jingxuan Wu,Yaxin Zhou,Ivor Tsang*

Main category: cs.AI

Selection reason: 研究不可靠反馈对工具使用agent的影响，直接相关agent工具使用与验证。

TL;DR: 当反馈不可靠时，工具增强型代理可能比无反馈代理表现更差，表明干净工具增益可能高估工具价值。


<details>
  <summary>Details</summary>
Motivation: 评估工具增强型代理在不可靠反馈下的表现，并揭示干净工具增益潜在的高估问题。

Method: 通过受控的匹配循环比较，固定代理循环、提示、动作空间和解码，仅改变观察的忠实性、误导性或缺失性。

Result: 误导性反馈导致价值反转（如HotpotQA上F1从44.8降至4.7），反转在更强干净检索下仍存在，但可被后续干净证据部分修复；早期轨迹信号可预测失败，但简单修复受限。

Conclusion: 干净工具增益可能高估工具价值，评估工具增强代理时需要匹配无反馈回退控制。

中文摘要：工具增强型代理通常通过其在可靠外部反馈下的增益来评估。然而，这些增益留下了一个关键的反事实问题：当反馈不可靠时，代理是否最好不接收任何任务证据？我们通过一个受控的匹配循环比较来研究这个问题，该比较固定了代理循环、提示、动作空间和解码，仅改变返回的观察：忠实的、误导的或缺失的。在问答和事实验证任务中，持续的误导性反馈产生了价值反转：从干净工具中获益的代理可能表现得比匹配的无反馈回退更差。在HotpotQA上，Qwen2.5-7B在干净检索下达到44.8 F1，无反馈下为22.3 F1，但在乱序检索下降至4.7 F1。这种反转在更强的干净检索和局部合理的干扰物下仍然存在，但当后续干净证据可以修复轨迹时减弱。早期轨迹信号可以预测许多失败，然而简单的修复仍然受限于回退：只有在暴露的回退可靠时，拒绝坏证据才有帮助。这些结果表明，干净工具增益可能高估工具价值，并且匹配的无反馈回退控制对于评估工具增强型代理是必要的。

Original abstract: Tool-augmented agents are typically evaluated by their gains under reliable external feedback. Yet these gains leave open a key counterfactual: when feedback is unreliable, would the agent be better off receiving no task evidence? We study this question with a controlled matched-loop comparison that fixes the agent loop, prompt, action space, and decoding, while varying only the returned observation: faithful, misleading, or absent. Across question answering and fact verification, persistent misleading feedback produces a value inversion: agents that benefit from clean tools can perform worse than the matched no-feedback fallback. On HotpotQA, Qwen2.5-7B reaches 44.8 F1 with clean retrieval and 22.3 F1 with no feedback, but drops to 4.7 F1 under shuffled retrieval. The inversion persists under stronger clean retrieval and locally plausible distractors, but weakens when later clean evidence can repair the trajectory. Early trajectory signals predict many failures, yet simple repairs remain fallback-limited: rejecting bad evidence helps only when the exposed fallback is reliable. These results show that clean-tool gains can overstate tool value, and that matched no-feedback fallback controls are necessary for evaluating tool-augmented agents.

</details>


### [24] [AutoRAS: Learning Robust Agentic Systems with Primitive Representations](http://arxiv.org/abs/2606.21445v1)
*Yang Yue,Xuancheng Zhu,Yuyang Ma,Guoshun Nan,Zihan Dou,Jingru Shan,Congyu Guo,Ji Zhang,Hua Wang,Jingfeng Zhang*

Main category: cs.AI

Selection reason: 自动设计鲁棒多agent工作流，可迁移至coding agent

TL;DR: 提出AutoRAS框架，通过生成符号原语并利用安全信号优化，实现鲁棒智能体系统的自动化设计，在普通和对抗设置下均表现最佳。


<details>
  <summary>Details</summary>
Motivation: 现有智能体系统设计忽视鲁棒性，易受外部对手和内部故障影响，需要自动化鲁棒设计方法。

Method: 将系统设计建模为符号原语序列生成，原语编码结构连接和行为动作，使用执行安全信号和流目标优化序列。

Result: 在普通和对抗设置下均达到最佳性能，攻击下性能退化最小，具有强可迁移性、稳定优化和跨原语稳定性。

Conclusion: AutoRAS有效提升系统鲁棒性，且成本效益良好。

中文摘要：智能体系统的自动化设计为扩展大型语言模型（LLMs）超越单智能体推理提供了一条有前景的路径。虽然先前的工作通过手工制作或自动生成的多智能体工作流提升了任务性能，但鲁棒性往往被视为事后考虑，使得系统容易受到外部对手和内部故障的影响。我们提出了AutoRAS，一个用于自动化设计鲁棒智能体系统的框架。AutoRAS将系统设计表述为生成一系列符号原语，这些原语共同编码结构连接性和行为动作，并学习使用执行导出的安全信号和基于流的序列级目标来优化这个序列。大量实验表明，AutoRAS在普通和对抗性设置下都达到了最佳性能，且在攻击下性能退化最小。进一步的分析显示了强大的可迁移性、稳定的优化行为、跨原语集的稳定性以及有利的成本权衡。我们的代码可在\href{https://github.com/guohezuy/AutoRAS}{\text{这个https URL}}获取。

Original abstract: The automated design of agentic systems offers a promising pathway for scaling large language models (LLMs) beyond single-agent reasoning. While prior work has advanced task performance through handcrafted or automatically generated multi-agent workflows, robustness is often treated as an afterthought, leaving systems vulnerable to external adversaries and internal failures. We propose AutoRAS, a framework for the Automated design of Robust Agentic Systems. AutoRAS formulates system design as generating a sequence of symbolic primitives that jointly encode structural connectivity and behavioral actions, and learns to optimize this sequence using execution-derived safety signals and flow-based sequence-level objectives. Extensive experiments show that AutoRAS achieves the best performance in both vanilla and adversarial settings, with the smallest performance degradation under attacks. Further analyses demonstrate strong transferability, stable optimization behavior, stability across primitive sets, and favorable cost trade-offs. Our code is available at $\href{https://github.com/guohezuy/AutoRAS}{\text{this https URL}}$.

</details>


### [25] [Composing Verifiable Conceptual Models via Building Blocks: Towards Design-Time Verification of Agentic AI Workflows](http://arxiv.org/abs/2606.21565v1)
*Noe Y. Flandre,Alexander C. Nwala,Philippe J. Giabbanelli*

Main category: cs.AI

Selection reason: 设计时验证agentic工作流，可直接用于coding agent

TL;DR: 提出一种设计时验证方法，通过12条结构规则检查代理工作流构建块的兼容性，实验证明能可靠检测设计缺陷。


<details>
  <summary>Details</summary>
Motivation: 当前平台缺乏设计时验证机制，导致类似建模与仿真中未验证概念模型交互一致性的问题。

Method: 将代理工作流建模为可重用构建块的组合，定义12条结构规则检查兼容性，并实现软件原型。

Result: 在两个公开数据集（48个有缺陷工作流和168个变体）上评估，验证器能可靠检测违规，即使通过结构变换（如任务拆分）掩盖缺陷。

Conclusion: 未来可结合社区构建块存储库，以设计安全的代理工作流。

中文摘要：代理式AI系统通过协调决策、工具和外部行动的工作流架构来编排多个基于LLM的代理。虽然当前的平台强调运行时保障，但在系统设计期间验证工作流方面几乎没有支持。从建模与仿真的角度来看，这一差距类似于组合概念模型而不验证其构建块是否一致地交互。我们提出了一种设计时验证方法，该方法将代理工作流建模为可重用构建块的组合，并通过十二条结构规则检查它们的兼容性。我们在一个软件原型中实现了这些规则，并使用两个公开数据集进行了评估：48个已知有设计缺陷的工作流和168个保留工作流逻辑但改变图结构的变体。结果表明，即使通过像在代理之间拆分任务这样的结构变换来掩盖有缺陷的设计，我们的验证器也能可靠地检测到违规。未来的工作可以将我们的验证与社区构建块存储库相结合，以组合安全的代理工作流。

Original abstract: Agentic AI systems orchestrate multiple LLM-based agents through workflow architectures that coordinate decisions, tools, and external actions. While current platforms emphasize runtime safeguards, little support exists for verifying workflows during system design. From a Modeling \& Simulation perspective, this gap is analogous to composing conceptual models without verifying whether their building blocks interact coherently. We propose a design-time verification approach that models agentic workflows as compositions of reusable building blocks and checks their compatibility through twelve structural rules. We implemented these rules in a software prototype and evaluated them using two openly released datasets: 48 workflows with known design flaws and 168 variants that preserve workflow logic but alter graph structure. Results show that our verifier reliably detects violations even when flawed designs are obscured through structural transformations such as splitting tasks between agents. Future works could combine our verification with community repositories of building blocks to compose safe agentic workflows.

</details>


### [26] [Counsel: A Meta-Evaluation Dataset for Agentic Tasks](http://arxiv.org/abs/2606.21627v1)
*Sashank Pisupati,Henry Broomfield,Eujeong Choi,Antonia Calvi,Charlie Wang,Roman Engeler,Max Bartolo,Patrick Lewis*

Main category: cs.AI

Selection reason: 提出agentic任务元评估数据集，对coding agent评估方法有参考价值

TL;DR: 提出Counsel数据集，用于元评估LLM评判者在智能体任务中的批评质量，发现模型能力和推理努力均提升人类一致性。


<details>
  <summary>Details</summary>
Motivation: 智能体评估中人工标注耗时，依赖LLMJ但缺乏对其批评质量的评估。

Method: 构建Counsel数据集，包含开源LLMJ对两个基准测试的批评及人工元评估，并统计分析。

Result: 最强LLMJ在位置和推理上分别达到约88%和65%的人类一致性；模型能力和推理努力均提升一致性。

Conclusion: Counsel数据集有助于改进LLM评判者的对齐，促进开源研究。

中文摘要：随着智能体系统处理日益复杂的多步任务，评估其轨迹成为一个主要瓶颈——对流行的智能体基准测试中单个轨迹进行人工标注可能需要数小时，这使得扩展评估以衡量性能或整理训练数据变得困难。这促使广泛依赖自动化方法，例如使用LLM作为评判者（LLMJ）在过程和结果层面大规模地批评智能体，然而LLMJ批评的可靠性常常未被衡量。在此，我们引入Counsel，这是第一个针对智能体任务的元评估公共数据集。Counsel包含来自开源LLMJ在两种智能体基准测试（tau-bench（客户支持智能体）和DA-Code（编码智能体））上的过程级批评，以及对这些批评的人工元评估。人工标注者将每个标记错误的批评标记为“完全正确”、“位置正确但推理不佳”或“不应标记”，并实现了可靠的人工间一致性（Krippendorff's alpha为0.78）。生成的数据集按人类对齐度对LLMJ批评进行分层，涵盖轨迹内错误位置和推理质量，为校准、改进或训练用于智能体的LLMJ提供有价值的数据。比较开源评判者，我们发现更强的评判者模型和更多的推理努力都能改善与人类的一致性，最强评判者在位置上达到约88%的一致性，在推理上达到约65%。Counsel使用开源模型生成，并采用宽松许可供社区广泛使用，我们希望这将促进对基于LLM的智能体系统评估者的严格研究和改进对齐。

Original abstract: As agentic systems tackle increasingly complex multi-step tasks, evaluating their trajectories presents a major bottleneck - human annotation of a single trajectory on popular agentic benchmarks can take hours, making it difficult to scale evaluations for measuring performance or curating training data. This has driven widespread reliance on automated approaches such as LLM-as-a-judge (LLMJ) to critique agents at the process and outcome-levels at scale, however, the soundness of LLMJ critiques often goes unmeasured. Here, we introduce Counsel, the first public dataset of meta-evaluations for agentic tasks. Counsel consists of process-level critiques from open-weight LLMJs on two agent benchmarks: tau-bench (customer support agents) and DA-Code (coding agents), and human meta-evaluations of these critiques. Human annotators label critiques on each flagged error as "spot on", "correct location but poor reasoning", or "should not have flagged", achieving reliable inter-annotator agreement (Krippendorff's alpha of 0.78). The resulting dataset stratifies LLMJ critiques by human alignment across both error location within a trajectory and reasoning quality, serving as valuable data to calibrate, improve, or train LLMJs for agents. Comparing open-weight judges, we find that more capable judge models and more reasoning effort both enabled improved human agreement, with the strongest judge reaching ~88% agreement on location and ~65% on reasoning. Counsel is generated using open-weight models and is permissively licensed for broad community use, which we hope will enable rigorous study and improved alignment of LLM-based evaluators for agentic systems.

</details>


### [27] [ChainWorld: Composing Long-Horizon Desktop Workloads from Atomic OSWorld Tasks](http://arxiv.org/abs/2606.21654v1)
*Vincent Siu,Manasi Sharma,Dawn Song,Daniel Yue Zhang,Chenguang Wang*

Main category: cs.AI

Selection reason: 长程桌面任务benchmark，可启发代码agent的长程执行评估

TL;DR: 论文提出ChainWorld基准，通过组合原子任务生成长时域桌面工作负载，发现当前代理在单轮和多方轮评估中表现不佳，且失败模式不同。


<details>
  <summary>Details</summary>
Motivation: 现有计算机使用代理评估仅关注原子桌面任务，忽略了现实桌面工作需跨多目标维持状态的要求，存在评估差距。

Method: 提出ChainWorld，通过方向性兼容搜索将原子OSWorld任务组合成任务链（347条，长度2-4），并设计单轮（所有任务一次提示）和多轮（逐步揭示）两种评估协议。

Result: 在四个代理中，最大链完成率仅31%；多轮评估提升三个模型性能，但两种协议仍具挑战；单轮失败主因工件精度，多轮失败主因会话管理问题（碎判进度、后期脱节）。

Conclusion: 当前计算机使用代理在长时域桌面工作负载上表现有限，多轮协议提供不同挑战，需关注状态保持和会话管理能力提升。

中文摘要：计算机使用代理几乎仅在原子桌面任务上进行评估，但现实桌面工作需要在多个目标之间维持状态。我们通过ChainWorld研究这一差距，该方法通过方向性兼容搜索将原子OSWorld任务组合成长时域桌面工作负载，同时保留源评估器。生成的工作负载包含347个长度为2到4的链，并比较同一任务序列的两种呈现方式。在单轮评估中，所有任务一起呈现在一个提示中。在多轮评估中，任务一次一个地揭示。在四个当前的计算机使用代理中，最大链完成率为31%。多轮评估提高了三个模型的完成率，但两种协议仍然具有挑战性。两种协议也暴露出不同的失败特征。单轮失败集中在工件精度上，而多轮失败更常反映会话管理问题，如碎片化进度和后期回合脱离。

Original abstract: Computer use agents are evaluated almost exclusively on atomic desktop tasks, but realistic desktop work requires sustaining state across multiple objectives. We study this gap with ChainWorld, which composes atomic OSWorld tasks into long horizon desktop workloads through directional compatibility search while preserving the source evaluators. The resulting workload contains 347 chains of length two to four and compares two renderings of the same task sequence. In single turn evaluation, all tasks are presented together in one prompt. In multi turn evaluation, tasks are revealed one at a time. Across four current computer use agents, maximum chain completion is 31%. Multi turn evaluation improves completion for three models, but both protocols remain challenging. The two protocols also expose different failure profiles. Single turn failures concentrate on artifact precision, while multi turn failures more often reflect session management problems such as fragmented progress and later turn disengagement.

</details>


### [28] [Hallucination as Context Drift: Synchronization Protocols for Multi-Agent LLM Systems](http://arxiv.org/abs/2606.21666v1)
*Carson Rodrigues*

Main category: cs.AI

Selection reason: 多agent系统上下文同步协议，可迁移至多代码agent协作

TL;DR: 提出上下文分歧分数（CDS）和共享状态验证协议（SSVP），通过选择性同步减少多智能体LLM系统中的幻觉，避免全广播的污染效应。


<details>
  <summary>Details</summary>
Motivation: 多智能体LLM系统中的幻觉不仅源于模型缺陷，还源于智能体间知识状态的分歧（上下文漂移），全广播同步反而加剧幻觉。

Method: 定义CDS量化知识状态差异，提出SSVP让智能体定期交换压缩状态摘要并标记高分歧条件，在推理前避免矛盾。

Result: SSVP在旅行领域显著降低幻觉率（HR 0.463 vs. 全广播0.658），且API调用减少58%；在软件领域幻觉率均低于0.2，污染效应不复现。

Conclusion: 幻觉缓解可视为分布式系统问题，上下文同步应成为多智能体LLM设计的基本原语。

中文摘要：多智能体LLM系统经常产生不能仅由模型缺陷解释的幻觉输出。这类失败的一个重要部分并非源于模型能力不足，而是源于上下文漂移：并发智能体之间内部知识状态的分歧。当智能体在共享世界状态不匹配或过时的情况下进入协作任务时，它们的联合推理会产生表现为幻觉的矛盾。我们定义了上下文分歧分数（CDS），这是一种轻量级标量度量，量化了跨空间、时间和任务维度的智能体对之间的知识状态差异，并提出了共享状态验证协议（SSVP），它允许智能体定期交换压缩状态摘要，并在联合推理之前标记高分歧条件。我们使用Claude Haiku在两个领域（多智能体旅行和软件项目规划）评估了SSVP。在8个场景中的受控实验（旅行条件n=30，软件条件n=10）中，朴素的全广播同步将幻觉率提高了34%，高于无同步基线（HR：0.658对0.492，p=0.0022，d=1.18），这是传播错误智能体状态带来的污染效应。SSVP避免了这种失败模式，同时显示出适度、一致的降低（HR：0.463，d=0.30），并且在使用API调用减少58%的情况下，实现了显著低于全广播的幻觉率（p=0.0005，d=1.47）。在软件领域，污染效应没有复现，所有条件都收敛到低HR（<0.2），这证实了它特定于那些一个错误共享信念会级联到多个评估维度的任务。我们的结果将幻觉缓解重新构建为一个分布式系统问题，并将上下文同步确立为多智能体LLM设计中的一类基本原语。

Original abstract: Multi-agent LLM systems routinely produce hallucinated outputs that cannot be explained by model deficiencies alone. A significant class of these failures arises not from model incapacity but from context drift: the divergence of internal knowledge states between concurrent agents. When agents enter a collaborative task with mismatched or stale representations of shared world state, their joint reasoning produces contradictions that manifest as hallucination. We define the Context Divergence Score (CDS), a lightweight scalar metric quantifying knowledge-state discrepancy between agent pairs across spatial, temporal, and task dimensions, and propose the Shared State Verification Protocol (SSVP), which lets agents periodically exchange compressed state summaries and flag high-divergence conditions before joint reasoning. We evaluate SSVP across two domains (multi-agent travel and software project planning) using Claude Haiku. In controlled experiments (n=30 per condition, travel; n=10, software) across 8 scenarios, naive full-broadcast synchronization increases hallucination rate by 34% above the no-sync baseline (HR: 0.658 vs. 0.492, p=0.0022, d=1.18), a contamination effect from propagating erroneous agent states. SSVP avoids this failure mode while showing modest, consistent reduction (HR: 0.463, d=0.30) and achieves significantly lower hallucination than full-broadcast (p=0.0005, d=1.47) using 58% fewer API calls. The contamination effect does not replicate in the software domain, where all conditions converge to low HR (<0.2), confirming it is specific to tasks where one erroneous shared belief cascades across evaluation dimensions. Our results reframe hallucination mitigation as a distributed systems problem and establish context synchronization as a first-class primitive in multi-agent LLM design.

</details>


### [29] [Training the Orchestrator: A Supervised Approach to End-to-End PDDL Planning with LLM Agents](http://arxiv.org/abs/2606.21740v1)
*Rajesh Mangannavar,Zachary Coalson,Pranay Dugar,Prasad Tadepalli*

Main category: cs.AI

Selection reason: 训练orchestrator进行PDDL规划，降低推理成本，可直接应用于代码agent调度

TL;DR: HALO通过从验证器认证的轨迹中训练一个小型QLoRA网络并辅以硬编码规则，在多个规划基准上以极低成本达到或超越前沿LLM基线的性能。


<details>
  <summary>Details</summary>
Motivation: 现有方法依赖昂贵的前沿LLM在每个细化步骤进行调用，成本高昂，且验证器提供的强监督信号未被充分利用。

Method: HALO使用QLoRA微调的小型策略网络和三个硬编码规则，在21智能体动作空间上运行，从验证器认证的轨迹中学习编排决策。

Result: 在PlanBench、Natural Plan和经典规划基准上，HALO匹配或超过GPT-5-mini，接近Gemini-3-Flash，成本降低45倍和15倍，LLM调用减少40-50%。

Conclusion: HALO验证了利用验证器监督训练轻量级编排器的有效性，实现了低成本高效的计划生成，为自然语言到规划意图的转化提供了实用方案。

中文摘要：将自然语言规划意图转化为经过验证的计划是一个长期的挑战：人们用语言传达目标，而经典规划器需要正式的PDDL规范。最近的智能体框架通过在一个验证器检查的细化循环内部编排一组专门的修复智能体来弥合这一差距，但中心的编排器本身是一个提示驱动的前沿LLM，每次细化步骤都要支付前沿LLM API调用。我们提出HALO（混合智能体-学习编排器），它从外部验证器已认证为以有效计划结束的细化轨迹中训练编排器，跨越11个PDDL领域。HALO将一个小的QLoRA调整策略与三个用于平凡可判定选择的硬编码规则配对，并在扩展的21智能体动作空间上运行。与每一步都提示前沿LLM或从稀疏的回合结束奖励中学习编排器的方法不同，我们的关键观察是验证器已经提供了强有力的指导：每个被接受的轨迹都是一系列可证明正确的（状态，智能体）决策，可直接用作监督。在PlanBench、Natural Plan和经典规划基准上，HALO在成功率上匹配或超过GPT-5-mini提示基线，比更强的Gemini-3-Flash提示基线低三个百分点以内，将编排成本降低一个数量级以上（针对GPT-5-mini从$0.18到$0.004每个任务，大约便宜45倍；比Gemini-3-Flash便宜大约15倍），并将每回合的总LLM调用次数减少40%到50%。

Original abstract: Translating natural-language planning intent into verified plans is a longstanding challenge: people communicate goals in language, while classical planners require formal PDDL specifications. Recent agentic frameworks bridge this gap by orchestrating a pool of specialized repair agents inside a verifier-checked refinement loop, but the orchestrator at the centre is itself a prompted frontier LLM, paying a frontier-LLM API call at every refinement step. We present HALO (Hybrid Agent-Learned Orchestrator), which trains the orchestrator from refinement trajectories that an external verifier has certified as ending in valid plans, across 11 PDDL domains. HALO pairs a small QLoRA-tuned policy with three hardcoded rules for trivially decidable selections, and operates over an expanded 21-agent action space. Unlike approaches that prompt a frontier LLM at every step or learn an orchestrator from sparse end-of-episode rewards, our key observation is that the verifier already provides strong guidance: every accepted trajectory is a sequence of demonstrably correct (state, agent) decisions, directly usable as supervision. Across PlanBench, Natural Plan, and classical planning benchmarks, HALO matches or exceeds the GPT-5-mini prompted baseline on success rate, sits within three percentage points of the stronger Gemini-3-Flash prompted baseline, reduces orchestration cost by more than an order of magnitude (\$0.18 to \$0.004 per task against GPT-5-mini, roughly 45$\times$ cheaper; roughly 15$\times$ cheaper than Gemini-3-Flash), and cuts total LLM calls per episode by 40 to 50 percent.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [30] [What Context Does a Coding Agent Actually Need to Act?](http://arxiv.org/abs/2607.09691v1)
*Brian Sam-Bodden*

Main category: cs.LG

Selection reason: 直接研究coding agent编辑代码时真正需要哪些上下文，对agent设计有直接启发。

TL;DR: 编码智能体编辑代码时，真正需要的信息非常少：主要是被编辑代码本身，而非其摘要或周围上下文；压缩上下文可大幅降低token消耗，且温度0推理存在约9%的噪声。


<details>
  <summary>Details</summary>
Motivation: 研究编码智能体在编辑代码时实际需要多少上下文，以优化其效率，避免浪费资源处理无关信息。

Method: 将定位（finding）与执行（acting）分离，通过oracle固定定位，系统性地变化代码表示方式（包括自然语言摘要、UML骨架、压缩上下文等），在SWe-bench Verified上评估问题解决率，并控制温度和随机性。

Result: 代码本身的自然语言摘要几乎无效（4/45 vs 27/45）；周围上下文（UML替代）无显著提升（p=0.75）；压缩上下文可节省约2/3 token而不降低效果；温度0推理存在约9%的结果翻转噪声。

Conclusion: 编码智能体编辑代码所需上下文高度集中，主要依赖被编辑代码本身；压缩上下文是高效策略；研究需注意温度0推理的固有噪声底限。

中文摘要：一个现代编码智能体可以在其上下文窗口中容纳整个代码库。它的大部分阅读是浪费的——有趣的问题不是智能体可以使用多少上下文，而是它实际上需要什么。我们在关键时刻研究这个问题：当智能体必须编辑代码时。将寻找工作站点与执行动作分开，我们使用oracle固定定位，只改变代码的表示方式，并根据SWE-bench Verified上的真实问题解决情况来评估上下文。答案是极其简约的。信号存在于被编辑的代码本身：其自然语言摘要几乎无法回答源代码所回答的行为问题（4/45 vs. 27/45，保留仓库，独立评判），而且差距属于表示方式，而非摘要器——前沿模型的摘要得分与3B模型一样差。周围上下文也几乎不重要：在Verified的每个多文件实例中，在数据冻结前的协议下，将文件的其余部分渲染为UML骨架和签名所解决的问题数量并不比直接删除该剩余部分更多（N=70，精确McNemar检验p=0.75）。这是我们预先注册的假设，但它失败了。与此同时，压缩上下文以三分之一的token匹配整个文件：解决一个问题花费19K上下文token，而不是94K。该工具还产生了一个该领域应该注意的发现：温度0的API推理在字节相同的运行之间翻转了约9%的每个实例结果。这是在该基准上报告的每个小效应下的噪声底限，包括我们的。我们发布了该工具——经金标准验证的环境、每个实例的证明（每个参考编辑都可以从每个臂的上下文中表达）、确定性补丁构建以及预先注册的假设（我们公布其零假设）。

Original abstract: A modern coding agent can hold an entire repository in its context window. Most of its reading is wasted -- and the interesting question is not how much context an agent can use, but what it actually \emph{needs}. We study that question at the moment it matters most: when the agent must \emph{edit} code. Separating \emph{finding} the work site from \emph{acting} on it, we hold localization fixed with an oracle, vary only how the code is represented, and score context against real issue resolution on SWE-bench Verified. The answer is starkly minimal. The signal lives in the code being edited itself: natural-language summaries of it answer almost none of the behavioral questions that the source answers ($4/45$ vs.\ $27/45$, held-out repositories, independent judge), and the gap belongs to the representation, not the summarizer -- a frontier model's summaries score exactly as poorly as a 3B model's. The surrounding context hardly matters either: across every multi-file instance in Verified, under a protocol frozen before any data, rendering a file's remainder as UML skeletons and signatures resolves no more issues than deleting that remainder outright ($N{=}70$, exact McNemar $p{=}0.75$). That was our registered hypothesis, and it failed. Compressed context, meanwhile, matches whole files at a third of the tokens: a resolved issue costs $19$K context tokens, not $94$K. The instrument also yielded a finding the field should keep: temperature-0 API inference flips ${\sim}9\%$ of per-instance outcomes between byte-identical runs. That is a noise floor under every small effect reported on this benchmark, including ours. We release the instrument -- gold-validated environments, per-instance proof that every reference edit is expressible from every arm's context, deterministic patch construction, and pre-registered hypotheses whose nulls we publish.

</details>


### [31] [Task-Differentiated Atomic Skill Expansion and Routing for Continual Learning Across Highly Heterogeneous Tasks](http://arxiv.org/abs/2606.21307v1)
*Jiacheng Wang,Xinjia He,Qi Ding,Yutao Yang,Jie Zhou,Liyang Yu,Liang Dou,Qin Chen*

Main category: cs.LG

Selection reason: 持续学习方法，原子技能扩展与路由，可迁移至agent的持续学习。

TL;DR: 提出TASER框架，通过原子技能扩展和动态路由，在高度异质持续学习中提升可塑性并减少灾难性遗忘。


<details>
  <summary>Details</summary>
Motivation: 现有方法在高度异质任务设置下存在灾难性遗忘和容量分配效率低下的问题。

Method: TASER框架包含三个步骤：原子技能增量学习动态扩展容量、正交性增强技能检测保持技能独立性、技能动态路由通过任务条件门控组合相关技能。

Result: 在HeteroCLBench基准上，TASER优于多种强基线方法。

Conclusion: TASER通过提高可塑性和减少灾难性遗忘，有效应对高度异质的持续学习挑战。

中文摘要：持续学习（CL）通常在假设连续任务在语义上相关或结构上相似的条件下进行研究。然而，在高度异质的环境中，任务在推理模式和输入输出格式上存在显著差异，现有方法常常遭受灾难性遗忘和容量分配效率低下的问题。为了解决这一挑战，我们提出了任务差异化原子技能扩展与路由（\texttt{TASER}），这是一种CL框架，它联合决定每个任务引入多少新原子技能以及激活哪些技能。该框架首先使用原子技能增量学习，基于任务差异和模型不确定性动态扩展容量。然后应用正交性增强技能检测，确保这些技能在语义上保持独立且可独立重用。最后，技能动态路由机制通过轻量级任务条件门控组合任务相关技能。我们进一步引入了\texttt{HeteroCLBench}，这是一个高度异质的CL基准，包含9个认知维度下的19个多样化任务，遵循标准化的顺序协议。在\texttt{HeteroCLBench}上的实验表明，\texttt{TASER}通过提高可塑性并减少灾难性遗忘，始终优于强基线。

Original abstract: Continual learning (CL) is commonly studied under the assumption that sequential tasks are semantically related or structurally similar. However, in highly heterogeneous settings, where tasks differ substantially in reasoning patterns and input-output formats, existing methods often suffer from catastrophic forgetting and inefficient capacity allocation. To address this challenge, we propose Task-differentiated Atomic Skill Expansion and Routing (\texttt{TASER}), a CL framework that jointly determines how many new atomic skills to introduce for each task and which skills to activate. The framework first uses atomic skill incremental learning to dynamically expand capacity based on task divergence and model uncertainty. It then applies orthogonality-enhanced skill detection to ensure these skills remain semantically distinct and independently reusable. Finally, a skill dynamic routing mechanism composes task-relevant skills through lightweight task-conditioned gating. We further introduce \texttt{HeteroCLBench}, a highly heterogeneous benchmark for CL, comprising 19 diverse tasks across 9 cognitive dimensions under a standardized sequential protocol. Experiments on \texttt{HeteroCLBench} show that \texttt{TASER} consistently outperforms strong baselines by improving plasticity and reducing catastrophic forgetting.

</details>


### [32] [DataClaw0: Agentic Tailoring Multimodal Data from Raw Streams](http://arxiv.org/abs/2606.21337v1)
*Cong Wan,Zeyu Guo,Zijian Cai,Jiangyang Li,SongLin Dong,Lin Peng,Xiangyang Luo,Zhiheng Ma,Yihong Gong*

Main category: cs.LG

Selection reason: Agentic数据整理，涉及agent驱动pipeline，可启发agent数据处理。

TL;DR: 提出主动数据裁剪（Agentic Data Tailoring）范式，通过基于事实锚点的两阶段数据生成和SFT+GRPO训练DataClaw模型，实现高信息密度数据精炼，在视频生成、VQA和GUI导航等下游任务中表现优异。


<details>
  <summary>Details</summary>
Motivation: 大规模非结构化多模态数据的高熵问题导致人类知识获取和AI后训练效率低下，现有被动标注范式成本高、单调且无法挖掘深层程序逻辑。

Method: 提出Agentic Data Tailoring范式，采用两阶段流水线（基于确定性事实锚点的生成式语义合成）构建大规模多领域数据集，并使用SFT与GRPO协同训练DataClaw模型，同时构建专用基准DataClaw-val。

Result: 在视频生成、真实世界VQA和GUI导航任务上，DataClaw提供的高信息密度数据使得模型在有限训练数据条件下高效适应新任务。

Conclusion: 主动数据裁剪是一种有效的范式，DataClaw模型能够稳健地对齐复杂精炼意图，显著提升下游任务性能。

中文摘要：大规模非结构化多模态流遭受高'数据熵'的困扰，阻碍了高效的人类知识获取和高质量的AI后训练。现有的被动标注范式严重依赖启发式规则或通用VLM，成本高昂、单调，并且无法解锁原始数据中蕴含的深层程序逻辑。我们将数据处理提升为一种可学习的能力，提出向主动数据裁剪（Agentic Data Tailoring）的范式转变，主动精炼和结构化数据以符合多样化的用户和下游意图。为了克服训练这种高阶能力的数据稀缺瓶颈，我们设计了一个两阶段流水线，将生成式语义合成基于确定性事实锚点，从而生成了一个涵盖五个核心物理和数字领域的大规模数据集。在此基础上，DataClaw0-9B模型将监督微调（SFT）与组相对策略优化（GRPO）协同起来，实现了与复杂精炼和裁剪意图的稳健对齐。为了系统量化这一能力，我们构建了DataClaw0-val，这是第一个专门用于数据精炼的基准。关键的是，我们采用下游后训练作为最终的验证试金石。在视频生成、真实世界VQA和GUI导航上的评估证实，DataClaw0提供高信息密度的裁剪数据，促进了在有限训练数据条件下模型对新任务的高效适应。项目页面：https://czjdsg.github.io/MakeAnyData

Original abstract: Massive unstructured multimodal streams suffer from high "data entropy," impeding both efficient human knowledge acquisition and high-quality AI post-training. Existing passive annotation paradigms, heavily reliant on heuristic rules or general VLMs, are costly, monotonous, and fail to unlock the deep procedural logic embedded in raw data. We elevate data processing to a learnable capability, proposing a paradigm shift towards Agentic Data Tailoring, which actively refining and structuring data to align with diverse user and downstream intents. To overcome the data scarcity bottleneck in training such high-order capabilities, we design a two-stage pipeline grounding generative semantic synthesis in deterministic Factual Anchors, yielding a large-scale dataset spanning five core physical and digital domains. Building upon this, $\text{DataClaw}_0$-9B model synergizes Supervised Fine-Tuning (SFT) with Group Relative Policy Optimization (GRPO), achieving robust alignment with complex refinement and tailoring intents. To systematically quantify this capability, we construct $\text{DataClaw}_0$-val, the first benchmark dedicated to data refinement. Crucially, we adopt downstream post-training as the ultimate validation touchstone. Evaluations on video generation, real-world VQA, and GUI navigation confirm that $\text{DataClaw}_0$ delivers high-information-density tailored data, facilitating efficient model adaptation to new tasks under limited training data regimes. Project page: https://czjdsg.github.io/MakeAnyData

</details>


### [33] [Depth-Entropy Guided Sampling for Training-Free LLM Reasoning](http://arxiv.org/abs/2607.09693v1)
*Zibin Meng,Peng Xie,Kani Chen*

Main category: cs.LG

Selection reason: 训练-free推理方法，利用深度熵引导采样，可迁移至agent推理。

TL;DR: 提出DEGS，一种无需训练的测试时采样方法，利用Transformer层熵塌陷作为质量信号，在多个基准上达到SOTA，尤其擅长域外和困难任务。


<details>
  <summary>Details</summary>
Motivation: 现有强化学习提升推理能力需要昂贵训练和标注数据，而测试时采样可恢复RL收益，但现有方法仅利用输出似然，忽略内部动力学；我们探索层熵塌陷作为质量信号，旨在无需额外资源的情况下提升推理。

Method: 定义塌陷深度D(x)，结合序列似然构建联合目标π(x) ∝ p(x)^α exp(βD(x))，在MCMC幂采样框架（DEGS-MCMC）中采样。

Result: 在三个模型四个推理基准上，DEGS达到免训练SOTA，特别是域外和困难分割上增益显著，开销仅个位数时钟；在数学分割上略逊于GRPO，但在GPQA域外任务上超越所有模型的GRPO参考。

Conclusion: DEGS是一种训练免费、测试时的方法，无需奖励模型或标注数据，通过利用层熵塌陷有效提升推理能力，尤其适用于域外场景。

中文摘要：强化学习（RL）已成为提升大语言模型推理能力的主导范式，但它需要昂贵的训练、精心整理的数据和奖励信号。近期工作表明，在测试时从锐化的基模型分布中采样可以恢复大部分RL收益，然而现有方法仅依赖输出层似然，忽略了Transformer的内部前向传播动态。我们提出深度-熵引导采样（DEGS），一种无需训练、测试时的方法，利用逐层熵塌陷作为内在质量信号。我们观察到更强的推理者——包括RL后训练变体——表现出独特的“晚期塌陷”：通过logit透镜解码的熵在深层之前保持较高水平，然后才收敛。我们定义了每个序列的塌陷深度D(𝑥)和一个联合目标π(𝑥) ∝ p(𝑥)^α exp(βD(𝑥))，该目标将序列似然与深度-熵结构相结合，实例化在一个MCMC幂采样框架（DEGS-MCMC）中。在三个开源模型和四个推理基准上，这种近乎随机的每候选信号在采样轨迹中累积，达到了最先进的免训练精度，其中在域外和更困难的分割上增益最大——恰好是似然单独失效的地方——且只有个位数的时钟开销。DEGS在GRPO训练的数学分割上略微落后于内部GRPO参考，但在三个模型的GPQA域外任务上均超越它，且无需任何训练、奖励模型或标注数据。

Original abstract: Reinforcement learning (RL) has become the dominant paradigm for improving the reasoning capabilities of large language models, but it requires expensive training, curated data, and reward signals. Recent work shows that sampling from sharpened base-model distributions at test time recovers much of the RL gain, yet existing methods rely solely on output-layer likelihoods and ignore the transformer's internal forward-pass dynamics. We introduce Depth-Entropy Guided Sampling (DEGS), a training-free, test-time method that exploits layer-wise entropy collapse as an intrinsic quality signal. We observe that stronger reasoners -- including RL-posttrained variants -- exhibit a distinctive "late collapse": logit-lens decoded entropy stays elevated until deeper layers before converging. We define a per-sequence collapse depth $D(\mathbf{x})$ and a joint objective $π(\mathbf{x}) \propto p(\mathbf{x})^α\exp(βD(\mathbf{x}))$ that combines sequence likelihood with this depth-entropy structure, instantiated inside an MCMC power-sampling framework (DEGS-MCMC). Across three open-weight models and four reasoning benchmarks, this near-chance per-candidate signal compounds over the sampling trajectory into state-of-the-art training-free accuracy, with gains largest out of domain and on the harder splits -- exactly where likelihood alone falls short -- at single-digit-percent wall-clock overhead. DEGS narrowly trails an in-house GRPO reference on the math splits GRPO was trained for, yet surpasses it out of domain on GPQA for all three models, without any training, reward model, or labeled data.

</details>


### [34] [Decodable but Not Faithful: Coupling Natural-Language Rationales to Programmatic Verifiers](http://arxiv.org/abs/2606.21678v1)
*Vatsal Ananthula,Adarsh Kumarappan*

Main category: cs.LG

Selection reason: 解释忠实性与程序验证器耦合机制，对代码agent的验证方法有启发

TL;DR: 提出验证器耦合推理框架，发现一致性训练虽能使验证器信息可解码，但不保证忠实生成；实验表明存在可解码性与忠实性之间的差距。


<details>
  <summary>Details</summary>
Motivation: 语言模型生成的解释可能不忠实于内部推理，现有方法缺乏对忠实性的保障。

Method: 提出verifier-coupled reasoning框架，插入内联声明并训练一致性头预测验证器输出。

Result: 在LeanCheck和KataGo上实现高可解码性，但在代码设置中解释不忠实；通过激活补丁等实验证实因果影响但忠实性不足。

Conclusion: 一致性损失是有效的诊断和表示塑造工具，但不是忠实推理的充分条件。

中文摘要：语言模型可以为其预测生成看似合理的理由，但这些解释可能并不忠实地代表模型的内部推理。我们提出了验证器耦合推理框架，该框架在推理轨迹中插入内联声明，并训练一个辅助一致性头来从理由跨度隐藏状态预测程序化验证器的输出。核心发现是可解码性与忠实性之间存在差距：一致性训练可靠地使验证器信息能够从理由表示中解码，但可解码性并不保证忠实生成。在LeanCheck（形式定理证明）中，仅理由和仅证明池化在反事实冲突下实现了完美的方向分离。在KataGo（围棋引擎）中，评论跨度以81%的准确率编码10路胜率桶。然而在代码设置中，模型实现了98.6%的耦合，但其生成的解释仍然不忠实：流畅的散文，正确的结构化声明，但描述了无关的算法；一个受控的预训练与从头训练的比较表明，差距不是由能力驱动的。合成激活补丁证实了因果影响（73-89%对比31%基线），FEVER显示仅证据池化以原始准确度为代价隔离了真正的证据敏感性，而逐声明分析表明，一致性损失对细粒度声明的益处大于二元声明。这些结果证明，一致性损失是有效的诊断和表示塑造工具，但不是忠实推理的充分条件。

Original abstract: Language models can generate plausible rationales for their predictions, but these explanations may not faithfully represent the model's internal reasoning. We propose verifier-coupled reasoning, a framework that inserts inline claims into reasoning traces and trains an auxiliary consistency head to predict programmatic verifier outputs from rationale-span hidden states. The central finding is a gap between decodability and faithfulness: consistency training reliably makes verifier information decodable from rationale representations, but decodability does not guarantee faithful generation. In LeanCheck (formal theorem proving), rationale-only and proof-only pooling achieve perfect directional separation under counterfactual conflict. In KataGo (Go engine), commentary spans encode 10-way win-rate buckets at 81% accuracy. Yet in a code setting, the model achieves 98.6% coupling while its generated explanations remain unfaithful: fluent prose with correct structured claims, but describing unrelated algorithms; a controlled pretrained-vs-from-scratch comparison shows the gap is not capacity-driven. Synthetic activation patching confirms causal influence (73-89% vs. 31% baseline), FEVER reveals that evidence-only pooling isolates genuine evidence sensitivity at the cost of raw accuracy, and per-claim analysis shows that consistency loss disproportionately benefits fine-grained claims over binary ones. These results establish that consistency losses are effective diagnostics and representation-shaping tools, but not sufficient conditions for faithful reasoning.

</details>


### [35] [Beyond the Next Step: Variable-Length Latent World Models for Long-Horizon Planning](http://arxiv.org/abs/2606.21775v1)
*Tianqi Du,Qi Zhang,Yifei Wang,Yisen Wang*

Main category: cs.LG

Selection reason: 提出可变长度潜在世界模型用于长程规划，属于通用agent规划新方法，可迁移至coding agent的长程代码生成规划。

TL;DR: 提出变长潜在世界模型（VLWMs），通过直接建模可变长度动作序列的动力学并结合课程训练，有效改进了长时域预测与规划，在基准任务上平均提升13%。


<details>
  <summary>Details</summary>
Motivation: 现有潜在世界模型依赖单步预测递归展开进行长时域规划，导致复合误差以及训练目标与下游规划任务之间的不匹配。

Method: 提出VLWMs框架，学习基于可变长度动作序列预测未来潜在状态，并采用课程训练策略逐步扩展动作时域，测试时设计定制规划方法以充分利用变长预测能力。

Result: 在长时域控制任务上，VLWMs比最先进的LeWM平均改进13%，尤其在需要扩展规划的任务上提升显著。

Conclusion: VLWM提供了一种简单有效的范式来改进潜在世界模型中的长时域预测与规划。

中文摘要：最近，世界模型作为一种有前景的范式出现，通过学习基于观测和动作估计未来环境状态的预测模型来构建智能体。特别是，JEPA风格的潜在世界模型通过在学习紧凑表示空间中的动作条件动力学，提供了像素空间预测的高效替代方案。然而，现有的潜在世界模型通常依赖于单步预测，必须递归地展开以进行长时域规划，这导致复合误差以及训练目标与下游规划任务之间的不匹配。为了解决这一限制，我们提出了变长潜在世界模型（VLWMs），一个学习基于可变长度动作序列预测未来潜在状态的框架。VLWMs不只在单步转移上训练，而是直接建模时间上扩展的动力学，允许同一个预测器评估不同时域的动作计划。我们进一步引入了课程训练策略，逐步扩展动作时域，稳定从短程动力学到长程预测的优化。在测试时，我们针对VLWMs设计了规划方法，以更好地利用其变长预测能力。在长时域控制任务上的实验表明，VLWMs显著改进了潜在空间世界模型，在不同数据集上比目前最先进的LeWM平均提高了13%，特别是在需要扩展规划的任务上收益更大。这些结果表明VLWM提供了一种简单而有效的范式来改进潜在世界模型中的长时域预测与规划。

Original abstract: Recently, world models have emerged as a promising paradigm for building intelligent agents by learning predictive models that estimate future environment states conditioned on observations and actions. In particular, JEPA-style latent world models provide an efficient alternative to pixel space prediction by learning action-conditioned dynamics in compact representation spaces. However, existing latent world models typically rely on one-step prediction and must be recursively rolled out for long-horizon planning, which leads to compounding errors and a mismatch between training objectives and downstream planning tasks. To address this limitation, we propose Variable-length Latent World Models (VLWMs), a framework that learns to predict future latent states conditioned on action sequences of variable lengths. Instead of training only on one-step transitions, VLWMs directly model temporally extended dynamics, allowing the same predictor to evaluate action plans over different horizons. We further introduce a curriculum training strategy that progressively expands the action horizon, stabilizing optimization from short-range dynamics to long-range prediction. At test time, we design planning methods tailored to VLWMs to better exploit their variable-length predictive capabilities. Experiments on long-horizon control tasks show that VLWMs significantly improve latent space world models, achieving 13\% average improvement over the state-of-the-art LeWM across different datasets, with especially large gains on tasks requiring extended planning. These results suggest that VLWM provides a simple yet effective paradigm for improving long-horizon prediction and planning in latent world models.

</details>


<div id='cs.CL'></div>

# cs.CL [[Back]](#toc)

### [36] [Scalable Hierarchical Attention Transformers for Multi-Turn Jailbreak Detection in Long Conversations](http://arxiv.org/abs/2606.21082v1)
*Chenhui Hu,Muhammed Salih,Sudipto Guha,Subramanian Srinivasan*

Main category: cs.CL

Selection reason: 多轮越狱检测，涉及对话agent安全，可迁移至agent安全防护

TL;DR: 提出一种层次化检测器用于多轮越狱攻击检测，通过紧凑轮次表示和轻量对话模块实现高效跨轮推理，在基准上F1达0.9394，优于Claude Opus 4.7且误报率减半。


<details>
  <summary>Details</summary>
Motivation: 多轮越狱攻击通过跨轮次逐步扩散不安全意图，规避单轮审核，现有检测方法缺乏高效的对话级分类能力。

Method: 构建层次化检测器：编码单轮成紧凑表示，再通过轻量对话模块（结合交叉注意力和自注意力）捕获对话动态并聚焦关键证据。

Result: 在14,038对话基准上F1=0.9394，比Claude Opus 4.7高0.07，误报率减半；消融证实各组件有效，交叉+自注意力降低误报率2.26%。

Conclusion: 提出的层次化检测器有效解决多轮越狱检测，兼顾效率和准确性，为对话安全提供新方案。

中文摘要：多轮越狱攻击可以通过逐步升级、重新措辞和角色操纵等方式，将不安全意图分散在对话中，从而规避轮次级别的审核。我们将多轮越狱检测视为一个对话级别的分类问题，并引入了一种高效的层次化检测器，该检测器避免了昂贵的长时间上下文拼接，同时保留了跨轮推理能力。该模型对单个轮次进行编码以形成紧凑的轮次表示，并应用一个轻量级对话模块，该模块捕获对话动态，并在需要时选择性地关注细粒度证据。在一个包含14,038个对话的具有挑战性的评估基准上，我们的方法达到了0.9394的F1分数，比最强的竞争基线Claude Opus 4.7高出0.07，同时将其误报率降低了一半。消融研究证实，每个架构组件都有实质性贡献，其中在对话模块中结合交叉注意力和自注意力，比仅使用自注意力的变体在误报率上降低了2.26个百分点。

Original abstract: Multi-turn jailbreaks can evade turn-level moderation by spreading unsafe intent across a dialogue through gradual escalation, reframing, and role manipulation. We address multi-turn jailbreak detection as a conversation-level classification problem and introduce an efficient hierarchical detector that avoids expensive long-context concatenation while retaining cross-turn reasoning. The model encodes individual turns to form compact turn representations and applies a lightweight conversation module that captures dialogue dynamics and selectively attends to fine-grained evidence when needed. On a challenging evaluation benchmark of 14,038 conversations, our approach achieves an F1 of 0.9394, outperforming Claude Opus 4.7, the strongest competing baseline, by 0.07 while halving its false-positive rate. Ablation studies confirm that each architectural component contributes meaningfully, with combining cross-attention and self-attention in the conversation module yielding a 2.26 percentage point reduction in false-positive rate over the self-attention-only variant.

</details>


### [37] [AdaMem: Learning What to Remember for Personalized Long-Horizon LLM Agents](http://arxiv.org/abs/2606.21144v1)
*Xingyu Chen,Rui Wang,Zhaopeng Tu,Liefeng Bo*

Main category: cs.CL

Selection reason: 直接针对LLM agent长期记忆，提出自适应记忆策略，通用且可迁移。

TL;DR: 提出AdaMem，通过角色感知和反馈学习自适应记忆策略，在减少记忆体积的同时提升长期交互中的问答准确率。


<details>
  <summary>Details</summary>
Motivation: 现有LLM智能体记忆系统试图记住所有内容，导致记忆膨胀，在有限上下文和推理成本下降低个性化交互质量。

Method: AdaMem维护基于角色的记忆策略，通过每周QA反馈进行轻量级自我反思和失败回滚来优化记忆内容。

Result: 在AdaMem-Bench上，AdaMem相比均匀基线实现最高+9.0%的QA准确率提升，同时内存体积减少9%。

Conclusion: 自适应记忆策略能有效缓解记忆膨胀，提升长期个性化交互的性能和效率。

中文摘要：大型语言模型（LLM）智能体的长期记忆系统通常试图“记住一切”，均匀地提取记忆以保留尽可能多的事实。然而在生产环境中，推理成本和有限的上下文预算使得这一点难以实现：除了将原始对话整合到记忆中，智能体还必须发挥“写入控制”，高效地只保留每个用户真正关心的信息。否则，长期个性化交互会遭受“记忆膨胀”，无关的琐事挤占有用信息，逐渐降低问答准确率。我们认为值得记忆的内容取决于角色，并提出了AdaMem（自适应记忆），一种从反馈中学习为每个用户记住什么的方法。AdaMem维护一个结构化的、特定于角色的记忆策略，并通过轻量级的、补丁式的自我反思步骤以及失败回滚，从每周的问答反馈中优化该策略。为了研究这一场景，我们构建了AdaMem-Bench，一个模拟数周交互并每周进行问答的基准。在两种提取模型和两种反馈模式下，AdaMem相比于均匀的Mem0基线，问答准确率提高了最多+9.0%，同时内存体积减少了9%。

Original abstract: Long-term memory systems for Large Language Model (LLM) agents typically try to \emph{remember everything}, extracting memories uniformly to retain as many facts as possible. In production, however, inference cost and finite context budgets make this untenable: beyond consolidating raw dialogue into memory, an agent must exert \emph{write control}, efficiently keeping only the information each user actually cares about. Otherwise, long-horizon personalized interactions suffer \emph{memory bloat}, where irrelevant trivia crowds out useful information and steadily erodes question-answering (QA) accuracy. We argue that what is worth remembering is role-dependent, and propose \textbf{AdaMem} (Adaptive Memory), a method that \emph{learns what to remember} for each user from feedback. AdaMem maintains a structured, role-specific Memory Policy and refines it from weekly QA feedback through a lightweight, patch-style self-reflection step with failure rollback. To study this setting, we build \textbf{AdaMem-Bench}, a benchmark that simulates weeks of interaction with week-by-week QA. Across two extraction models and two feedback modes, AdaMem improves QA accuracy by up to \textbf{+9.0\%} over the uniform Mem0 baseline while shrinking memory volume by \textbf{9\%}.

</details>


### [38] [PrivacyAlign: Contextual Privacy Alignment for LLM Agents](http://arxiv.org/abs/2606.21710v1)
*Manveer Singh Tamber,Abhay Puri,Marc-Etienne Brunet,Perouz Taslakian,Jimmy Lin,Spandana Gella*

Main category: cs.CL

Selection reason: LLM agent隐私对齐方法，可迁移至代码agent的安全对齐

TL;DR: 本文提出PrivacyAlign数据集，通过人类注释中心化智能体隐私对齐，并引入注释条件奖励建模，使小型开源智能体更好地符合人类隐私规范。


<details>
  <summary>Details</summary>
Motivation: 现有AI智能体在隐私决策上往往与用户期望不一致，而隐私判断依赖于社会规范，但现有工作使用不可靠的代理进行训练和评估，缺乏以人类判断为中心的方法。

Method: 构建PrivacyAlign数据集（1350样本，3516条注释），基于人类注释训练LLM评判者，并提出注释条件奖励建模用于强化学习。

Result: 注释条件奖励训练的小型开源智能体在PrivacyAlign和现有隐私基准上显著提升，与人类隐私规范对齐更好。

Conclusion: 将人类判断置于中心，通过高质量标注和条件化奖励建模可以有效改善AI智能体的隐私对齐。

中文摘要：代表用户行事的AI智能体不断做出决策，为了让用户信任其智能体，这些决策必须与他们的实际期望一致。隐私对智能体来说是一个重要的对齐问题：智能体发出的每条消息、帖子或工具调用都是一种关于分享什么、与谁分享以及在何种条件下分享的上下文判断。由于此类判断取决于社会期望和规范，人类判断不仅标记隐私违规，还帮助定义它们。虽然现有工作依赖不可靠的代理进行训练和评估，我们将人类判断置于智能体隐私对齐的中心。我们引入了PrivacyAlign，这是一个包含1350个样本和来自599名不同标注者的3516条详细标注的数据集，覆盖当前LLM实际泄露隐私的多种场景，并用它来将对齐训练和自动评估建立在人类隐私规范之上。基于这些标注，我们首先展示，将LLM评判者条件化于人类注释和对同一提示的参考响应的解释，能使他们的判断更可靠。然后我们引入注释条件奖励建模，该模型在RL期间使用这些注释对新响应进行评分，并展示使用这种奖励训练的小型开源智能体更好地与人类隐私规范对齐，在PrivacyAlign和现有的智能体隐私基准测试上取得了显著提升。

Original abstract: AI agents acting on behalf of users are constantly making decisions, and for users to trust their agents, those decisions must align with what they actually want. Privacy is an important alignment problem for agents: every message, post, or tool call an agent makes is a contextual judgment about what is appropriate to share, with whom, and under which conditions. Because such judgments depend on social expectations and norms, human judgment does not merely label privacy violations but also helps define them. While existing work relies on unreliable proxies for both training and evaluation, we place human judgment at the center of agentic privacy alignment. We introduce PrivacyAlign, a dataset of 1,350 samples with 3,516 detailed annotations from 599 unique annotators across diverse scenarios where current LLMs actually leak, and use it to ground both alignment training and automated evaluation in human privacy norms. Building on these annotations, we first show that conditioning LLM judges on human annotations and explanations for reference responses to the same prompt makes their judgments more reliable. We then introduce annotation-conditioned reward modeling, which uses these annotations to score new responses during RL, and show that small open-weight agents trained with this reward better align with human privacy norms, with strong gains on PrivacyAlign and existing privacy benchmarks for agents.

</details>


### [39] [Denoising Iterative Self-Correction: Structured Verification Loops for Reliable LLM Reasoning](http://arxiv.org/abs/2606.21724v1)
*Shen Yin,David Ken,Joel Stremmel*

Main category: cs.CL

Selection reason: 迭代自校正推理框架，对agent规划验证循环有启发

TL;DR: 提出去噪迭代自我修正（DISC），通过迭代修正提高推理准确性，并在精度-召回权衡上显著优于现有方法。


<details>
  <summary>Details</summary>
Motivation: 现有语言模型的多步推理容易出错，且简单的修正方法可能损害原本正确的答案，缺乏精确控制修正过程的机制。

Method: DISC将验证问题输出视为噪声信号，通过多次验证-判断-修正循环逐步减少错误，并引入二值判断门控制修正精度，避免破坏正确部分。

Result: 在三个基准和四个模型上，DISC在精度-召回权衡上优于Chain-of-Verification和Self-Refine，在BIG-Bench Mistake上达到81.6%准确率，改进-退化比分别提升13倍和5倍。同时发现跨模型角色分配可减轻自我确认偏见。

Conclusion: DISC是一种有效的测试时推理修正方法，能精确控制修正过程，避免损害正确部分，并揭示了模型在证据矛盾和跨角色分配中的行为特性。

中文摘要：大型语言模型产生了流畅但往往不正确的多步推理，而简单的修正方法可能会降低原本正确的答案。我们提出了去噪迭代自我修正（Denoising Iterative Self-Correction, DISC），这是一种测试时程序，它将验证问题的输出视为关于解决方案可能被破坏的噪声测量。利用这些信号，DISC通过多次验证-判断-修正循环逐步减少错误，类似于传统的迭代去噪。一个二值判断门通过阻止会破坏已正确答案的重写来控制修正的精度，而验证器和修正器共同修复错误。我们使用两个配对的诊断指标来评估这一权衡：改进与退化比率（精度）和修复率（召回）。在三个基准（BIG-Bench Mistake、HotpotQA、GPQA Diamond）和四个模型上，DISC在精度-召回权衡上优于Chain-of-Verification和Self-Refine，在BIG-Bench Mistake（Sonnet 4.5）上达到81.6%的准确率，每次改进的退化比Chain-of-Verification少13倍，比Self-Refine少5倍。在GPQA Diamond上，我们识别出一个能力下限，低于该下限的判断者承认证据中的矛盾，但无法将这种认识转化为修正。我们进一步表明，跨模型角色分配——将验证和判断分配给与生成器不同的模型——减轻了自我确认偏见。

Original abstract: Large language models produce fluent but often incorrect multi-step reasoning, and naive correction methods risk degrading already-correct answers. We introduce Denoising Iterative Self-Correction (DISC), a test-time procedure that treats verification question outputs as noisy measurements of where a solution may be corrupted. Using these signals, DISC progressively reduces errors across multiple verify-judge-correct passes, analogous to traditional iterative denoising. A binary judgment gate controls correction precision by blocking rewrites that would damage already-correct answers while the verifier and corrector together repair errors. We evaluate this trade-off using two paired diagnostics: an improvement-to-degradation ratio (precision) and a repair rate (recall). Across three benchmarks (BIG-Bench Mistake, HotpotQA, GPQA Diamond) and four models, DISC dominates Chain-of-Verification and Self-Refine on the precision-recall trade-off, reaching 81.6% accuracy with 13x more improvements per degradation than Chain-of-Verification and 5x more than Self-Refine on BIG-Bench Mistake (Sonnet~4.5). On GPQA Diamond, we identify a capability floor below which judges acknowledge contradictions in evidence but cannot translate that recognition into a correction. We further show that cross-model role allocation -- assigning verification and judgment to a model different from the generator -- mitigates self-confirmation bias.

</details>


### [40] [CalVerT: Augmenting Agents with Calibrated Verifier Telemetry Improves Action and Learning in Knowledge-Intensive Tasks](http://arxiv.org/abs/2606.21777v1)
*Ashwin Vinod,Ying Ding,Elias Stengel-Eskin*

Main category: cs.CL

Selection reason: 通过校准验证器遥测增强agent状态，改善检索与推理决策，属于通用agent验证与自我改进方法，可迁移至coding agent的代码验证与反馈。

TL;DR: 提出CalVerT方法，通过向智能体状态添加校准置信度和基础验证分数，在无训练和训练设置中均能提升知识密集型问答的性能。


<details>
  <summary>Details</summary>
Motivation: LLM智能体在知识密集型问答中缺乏对答案状态（不确定、无支持或已完整）的认知，导致过早承诺错误答案或过度检索浪费计算资源。

Method: 引入校准验证器遥测（CalVerT），包括校准的自信心分数和基础验证器分数，将其作为智能体状态的额外信息，用于触发或抑制检索操作。

Result: 在四个QA基准测试中，CalVerT提高了F1分数，减少了过度依赖参数化知识的检索和冗余检索；在无训练和强化学习训练设置中均有效。

Conclusion: CalVerT通过提供更完整的状态空间信息，有效改进了LLM智能体在知识密集型问答中的准确性和效率，且可无缝融入现有框架。

中文摘要：在知识密集型问答中的LLM智能体在执行检索和推理操作时，对其当前答案是否不确定、无支持或已经完成缺乏完整认知。这导致两种失败模式：一是对自信但无支持的答案做出承诺，从而损害准确性；二是在已有证据足够时过度检索，造成计算浪费。为了让智能体对其操作的状态空间有更全面的了解，我们引入了校准验证器遥测（CalVerT），它用额外的遥测数据（校准的自信心分数和基础验证器分数）增强智能体的状态。我们表明，CalVerT可以在无训练和基于训练的设置中改进智能体。在四个QA基准测试中，我们发现，通过触发智能体过度依赖参数化知识时的检索，同时减少智能体拥有足够上下文回答问题的冗余检索，CalVerT提高了F1分数。我们证明，CalVerT可以在无训练的情况下增强现有的QA框架。此外，CalVerT也改进了经过训练的系统：仅通过使用遥测数据增强智能体的状态，与具有相同训练但没有CalVerT遥测的智能体相比，我们在强化学习后观察到了改进。

Original abstract: LLM agents in knowledge intensive question answering take retrieval and reasoning actions with incomplete knowledge about whether their current answer is uncertain, unsupported, or already complete. This produces two failure modes: committing to confident but unsupported answers, which hurts accuracy, and over-retrieving when the evidence in hand already suffices, resulting in wasted compute. To give agents a more complete picture of the state space they are operating in, we introduce calibrated verifier telemetry (CalVerT), which augments the agent's state with additional telemetry: a calibrated self-confidence score and a grounding verifier score. We show that CalVerT can improve agents in both training-free and training-based settings. On four QA benchmarks, we find that CalVerT raises F1 by triggering retrieval in cases where agents over-rely on parametric knowledge, while cutting redundant retrieval in cases where agents have sufficient context to answer. We show that CalVerT can augment existing QA frameworks without training. Moreover, CalVerT also improves trained systems: by simply augmenting an agent's state with telemetry, we observe improvements after reinforcement learning, as compared to an agent with identical training but no CalVerT telemetry.

</details>


<div id='cs.CY'></div>

# cs.CY [[Back]](#toc)

### [41] [Human-AI Interaction Requirements in Public Sector Procurements](http://arxiv.org/abs/2606.21247v1)
*Mateen A. Abbasi,Tommi Mikkonen,Sinna Pirinen,Aapo Koski*

Main category: cs.CY

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 本文提出一个针对公共部门ICT采购的人机交互需求分类法，以将伦理AI原则转化为可指定的采购需求。


<details>
  <summary>Details</summary>
Motivation: 公共部门采购AI系统时缺乏对人机交互需求的明确规范，导致伦理原则无法落地。

Method: 通过概念化人机交互为采购设计维度，提出一个交互需求分类法。

Result: 分类法使签约机构能通过采购工具指定和管理交互属性。

Conclusion: 该分类法支持伦理合规和可持续价值实现。

中文摘要：公共部门组织越来越多地采购人工智能赋能的ICT系统来支持决策和服务交付。尽管伦理AI框架强调透明度、可问责性和人类监督，但这些原则很少被转化为采购流程中的明确需求。因此，人机交互往往留给供应商的设计选择。本文将人机交互概念化为采购关键的设计维度，并提出了一个针对公共部门ICT采购定制的交互需求分类法。该分类法使签约机构能够通过采购工具指定和管理交互属性，支持伦理合规和可持续价值实现。

Original abstract: Public sector organizations increasingly procure AI-enabled ICT systems to support decision-making and service delivery. Although ethical AI frameworks emphasize transparency, accountability, and human oversight, these principles are rarely translated into explicit requirements in procurement processes. Consequently, human-AI interaction (HAI) is often left to vendor design choices. This paper conceptualizes HAI as a procurement-critical design dimension and proposes a taxonomy of interaction requirements tailored to public sector ICT procurement. The taxonomy enables contracting authorities to specify and govern interaction properties through procurement instruments, supporting both ethical compliance and sustainable value realization.

</details>


<div id='cs.RO'></div>

# cs.RO [[Back]](#toc)

### [42] [Decoupling the Declarative from the Procedural in Vision-Language-Action Models](http://arxiv.org/abs/2606.21496v1)
*Nikolaos Tsagkas,Andreas Sochopoulos,Chris Xiaoxuan Lu,Oisin Mac Aodha,Alexandros Kouris*

Main category: cs.RO

Selection reason: VLA解耦声明与程序知识，通用agent架构可迁移

TL;DR: 提出w$^{2}$VLA模型，通过重组信息流解耦陈述性和程序性知识，实现零样本技能迁移。


<details>
  <summary>Details</summary>
Motivation: 当前VLA模型无法解耦陈述性和程序性知识，导致对微小变化脆弱，零样本迁移受限。

Method: 提出w$^{2}$VLA，不将所有多模态令牌输入动作专家，而是用视觉、空间和技能信息以组合可解释方式调制机器人状态序列。

Result: 模块化方法成功解耦知识表示，实现鲁棒行为克隆和零样本技能迁移到不同未见物体。

Conclusion: w$^{2}$VLA在零样本技能迁移上表现优越。

中文摘要：在现实世界中部署通用机器人智能体需要可迁移的技能。具体来说，一个通过从特定物体演示中克隆行为而训练的策略必须能够泛化到该物体之外，否则数据收集需求将变得难以处理。最近，对预训练的数十亿参数视觉-语言模型（VLM）进行微调，首先在大规模机器人数据集上，然后在较少的情景特定演示上，已成为设计视觉-语言-动作（VLA）模型的主要范式。虽然这些策略在分布内实现了最先进的操作性能，但它们对微小的空间、语义和任务变化仍然脆弱。在这项工作中，我们解决了当前模型无法将陈述性知识（即概念和实体语义）与程序性知识（即如何做某事）从其参数中解耦的问题，这是对新颖物体进行零样本技能转移的根本瓶颈。为了解决这个问题，我们提出了w$^{2}$VLA，一种具有重组信息流的新VLA模型。与将所有多模态令牌从VLM编码器馈送到一个大型、不透明的基于Transformer的动作专家不同，我们的方法以组合且可解释的方式，用视觉、空间和技能信息调制机器人状态序列。与流行的、最先进的VLA不同，我们展示了我们的模块化方法成功解耦了知识表示，实现了鲁棒的行为克隆和在截然不同的、未见过的物体上前所未有的零样本技能转移能力。

Original abstract: Deploying generalist robotic agents in the real world requires transferable skills. Specifically, a policy trained to clone a behavior from object-specific demonstrations must generalize beyond that object, otherwise data collection requirements become intractable. Recently, fine-tuning of pre-trained billion-parameter Vision-Language Models (VLMs), initially on large-scale robot datasets and then on fewer scenario-specific demonstrations, has emerged as the predominant paradigm for designing Vision-Language-Action (VLA) models. While these policies achieve state-of-the-art manipulation performance in-distribution, they remain brittle to minor spatial, semantic, and task variations. In this work, we address the inability of current models to decouple the declarative (i.e., concepts and entity semantics) from the procedural knowledge (i.e., how to do something) encoded in their parameters, which is a fundamental bottleneck for zero-shot skill transfer to novel objects. To address this, we propose w$^{2}$VLA, a new VLA model with restructured information flow. Rather than feeding all multimodal tokens from the VLM encoder into a large, opaque transformer-based action expert, our approach modulates the robot state sequence with visual, spatial, and skill information in a compositional and interpretable manner. Unlike popular, state-of-the-art VLAs, we show that our modular approach successfully decouples knowledge representations, enabling robust behavior cloning and unprecedented zero-shot skill transfer capabilities across dissimilar, unseen objects.

</details>


### [43] [Imitation from Heterogeneous Demonstrations using Grounded Latent-Action World Models](http://arxiv.org/abs/2606.21672v1)
*Tianyou Wang,Anson Lei,Joe Watson,Ingmar Posner*

Main category: cs.RO

Selection reason: 潜动作世界模型对齐异构示范，对代码agent多源数据融合有启发

TL;DR: 提出一种基于接地潜在动作世界模型（GLAM）的方法，通过共享潜在动作空间从异构数据源学习视觉运动策略，显著提升任务成功率。


<details>
  <summary>Details</summary>
Motivation: 现有模仿学习受限于演示数据的规模和质量，而利用异构数据源时需手动对齐动作空间，缺乏通用性。

Method: 使用一对生成模型（GLAM）学习跨数据源的共享潜在动作空间，该空间通过预测未来观测来接地，并训练行为克隆策略将观测映射到潜在动作再解码为机器人动作。

Result: 在五个模拟和真实操作任务中，GLAM对齐的策略在数据稀缺下比基线和先前方法平均提升48%的任务成功率。

Conclusion: GLAM有效学习对齐的潜在动作空间，实现异构数据源的动作转移，为从异构数据学习提供了有效范式。

中文摘要：模仿学习已成为学习视觉运动策略的强大范式，但其泛化能力和稳定性受到演示数据规模和质量的限制。一个有前景的方向是利用更丰富但异构的数据源，这些数据源在动作空间上有所不同，并且通常完全没有动作标签。现有的结合异构数据源的协同训练方法依赖于启发式和手工设计的对齐技术。相比之下，我们认为动作表征应基于预测：对环境影响相同的动作，无论其来源如何，都应共享相同的表征。为此，我们通过使用接地潜在动作世界模型（GLAM）来实例化这一原则，该模型是一对生成模型，跨数据源具有共享的潜在动作空间，并通过跨源一致地预测未来观测来接地。该潜在动作空间用于训练下游的行为克隆（BC）策略，这些策略将观测映射到潜在动作，并将其解码回机器人动作，从而提供了一种从异构数据中学习的范式。实验上，我们证明GLAM成功学习了一个对齐的潜在动作空间，促进了跨数据源（无论是否有动作标签）的动作转移。在模拟和现实世界的五个操作任务中，GLAM对齐的策略显著优于BC基线和先前的潜在动作方法，在相同的数据稀缺设置下，任务成功率平均提高了48%。视频和代码可在https://viccccciv.github.io/glam/获取。

Original abstract: Imitation learning has emerged as a powerful paradigm for learning visuomotor policies, but its generalisation and stability are limited by the scale and quality of demonstration data needed. A promising direction is to leverage more abundant but heterogeneous data sources, which differ in action space and often lack action labels altogether. Existing co-training approaches that combine heterogeneous data sources rely on heuristic and hand-engineered alignment techniques. In contrast, we argue that action representations should be grounded in prediction: actions that produce the same effect on the environment should share the same representation, regardless of their sources. To this end, we instantiate this principle by using a grounded latent-action world model (GLAM), a pair of generative models with a shared latent action space across data sources that is grounded by predicting future observations consistently across sources. This latent action space is used to train downstream behavioural cloning (BC) policies which map observations to latent actions and decode them back to robot actions, providing a paradigm for learning from heterogeneous data. Empirically, we demonstrate that GLAM successfully learns an aligned latent action space that facilitates action transfer across data sources with and without action labels. Across five manipulation tasks in simulation and in the real world, GLAM-aligned policies significantly outperform BC baselines and prior latent-action methods, achieving an average of +48% improvement in task success rate with the same data-scarce setting. Videos and code are available at https://viccccciv.github.io/glam/.

</details>


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [44] [HPP: Hierarchical Programmatic Probing for Long Video Understanding by Decoupling Perception and Reasoning](http://arxiv.org/abs/2606.21734v1)
*Awais Rauf,Ahmed Hasssan,Greg Slabaugh*

Main category: cs.CV

Selection reason: 层次化程序探测长视频理解，agent规划方法可迁移至代码任务

TL;DR: HPP通过将长视频理解分解为层次化编程探测，解耦感知与推理，利用编码LLM交互式提取信息，显著提升长视频理解性能。


<details>
  <summary>Details</summary>
Motivation: 现有VLM将感知和推理耦合在单次前向传递中，受限于LLM的隐式多步推理能力，难以处理长视频的复杂时空动态。

Method: 提出层次化编程探测（HPP）框架，包括信息密度感知层次分割、后期交互语义检索和结构化探测函数，由编码LLM在交互环境中迭代执行多步策略，按需调用VLM进行局部感知。

Result: 在LongVideoBench、EgoSchema、VideoMME和MLVU上取得显著性能提升，验证了解耦感知与推理的有效性。

Conclusion: HPP通过编程式探测解耦感知与高阶推理，为长视频理解提供了一种高效且可扩展的范式。

中文摘要：理解长视频需要精细的感知以及对复杂的、长程的时空动态进行多步、高阶推理。视觉语言模型（VLM）将视频帧编码为视觉标记，并尝试在单次前向传递中隐式地同时执行感知和多步规划。然而，这种耦合的公式受限于LLM在其潜在表示中发现和执行多步策略的有限能力。为了解决这一瓶颈，我们提出了层次化编程探测（HPP），这是一个通过将长视频理解重新表述为对层次化分割视频的迭代、编程式探索，从而将语义感知与高阶时间推理解耦的框架。具体来说，一个具有编码能力的LLM在交互式编码环境中规划并执行多步策略，探测视频以获取信息，并按需调用VLM进行局部感知。为了使探测在长视频中可行，我们引入了三个组件：信息密度感知的层次分割、后期交互语义检索以及用于从粗到细时间定位的结构化探测函数。我们在LongVideoBench上验证了HPP，该基准需要精细感知和长程关系推理，结果表明通过迭代编程式探测解耦两者带来了显著的性能提升。在EgoSchema、VideoMME和MLVU上的进一步结果展示了我们方法在多样长视频基准上的有效性。

Original abstract: Understanding long videos requires fine-grained perception and multi-step, higher-order reasoning over complex, long-range spatio-temporal dynamics. Vision-language models (VLMs) encode video frames into visual tokens and attempt to perform both perception and multi-step planning latently, within a single forward pass. This coupled formulation, however, is bottlenecked by the LLM's limited capacity to discover and execute multi-step strategies in its latent representations. To address this bottleneck, we propose Hierarchical Programmatic Probing (HPP), a framework that decouples semantic perception from higher-order temporal reasoning by reformulating long video understanding as iterative, programmatic exploration of a hierarchically segmented video. Specifically, a coding-capable LLM plans and executes a multi-step strategy in an interactive coding environment, probing the video for information and invoking a VLM for localized perception on demand. To make probing tractable over long videos, we introduce three components: information-density-aware hierarchical segmentation, late-interaction semantic retrieval, and structured probing functions for coarse-to-fine temporal localization. We validate HPP on LongVideoBench, which requires both fine-grained perception and long-range relational reasoning, and show that decoupling the two via iterative programmatic probing yields substantial gains. Further results on EgoSchema, VideoMME, and MLVU demonstrate the effectiveness of our approach across diverse long-video benchmarks.

</details>


<div id='cs.HC'></div>

# cs.HC [[Back]](#toc)

### [45] [CORTIS: Text-Only Adaptation of Spoken Language Models for Task-Oriented Voice Agents](http://arxiv.org/abs/2606.21453v1)
*Youngwon Choi,Hyeonyu Kim,Taeyoun Kwon,Donghyuk Jung,Myeongkyun Cho*

Main category: cs.HC

Selection reason: 文本适配语音模型用于任务导向语音agent，方法可迁移

TL;DR: CORTIS是一种仅用文本任务监督微调口语语言模型的框架，使语音代理能在无配对语音-目标数据的情况下生成结构化输出，在性能上与ASR-LLM级联相当，且在噪声环境下更鲁棒。


<details>
  <summary>Details</summary>
Motivation: 任务导向语音代理中，ASR-LLM级联受转录错误影响，而口语语言模型需要昂贵的配对语音-目标标注，因此需要一种低成本且鲁棒的适应方法。

Method: 提出CORTIS框架，仅使用文本形式的任务监督对口语语言模型进行微调，使其在推理时直接处理语音输入并生成结构化输出，无需配对语音-目标数据。

Result: 在多个数据集上，CORTIS与匹配的ASR-LLM级联性能相当，并在声学退化条件下表现出更清晰的语义保持优势。

Conclusion: 文本仅微调口语语言模型是语音代理的一种实用适应策略，特别适用于配对数据难以获取的场景。

中文摘要：任务导向的语音代理需要将口语用户请求映射到结构化输出，如语义框架、可执行动作和函数调用。常见的方法是将ASR与基于文本的LLM级联，但转录错误可能会传播到下游的结构化输出生成，尤其是在噪声条件下。口语语言模型（SLM）提供了一种直接的基于语音的替代方案，但将它们适应新任务通常需要配对的语音-目标标注。受这一差距的启发，我们提出了CORTIS，一种用于任务导向语音代理的纯文本适应框架。CORTIS使用文本形式的任务监督对SLM进行微调，从而在推理时实现基于语音的结构化输出生成，而在适应过程中不需要特定任务的语音-目标标注。我们在两个Qwen2.5-Omni骨干网络和三个任务导向语音数据集（包括一个内部产品数据集）上评估了CORTIS，并将其与使用相同文本形式任务监督训练的匹配ASR-LLM级联进行了比较。结果表明，CORTIS与匹配的级联相比具有竞争力，并且在声学退化下展现出更明显的优势，特别是在保留高级任务语义方面。这些发现表明，当配对的语音-目标数据收集成本高昂时，SLM的纯文本微调可以作为一种实用的语音代理适应策略。

Original abstract: Task-oriented voice agents need to map spoken user requests to structured outputs such as semantic frames, executable actions, and function calls. A common approach is to cascade ASR with a text-based LLM, but transcription errors can propagate to downstream structured output generation, especially under noisy conditions. Spoken language models (SLMs) offer a direct speech-based alternative, yet adapting them to new tasks typically requires paired speech-target annotations. Motivated by this gap, we present CORTIS, a text-only adaptation framework for task-oriented voice agents. CORTIS fine-tunes SLMs using text-form task supervision, enabling speech-based structured output generation at inference time without task-specific speech-target annotations during adaptation. We evaluate CORTIS on two Qwen2.5-Omni backbones and three task-oriented speech datasets, including an in-house product dataset, and compare it with matched ASR-LLM cascades trained with the same text-form task supervision. Results show that CORTIS performs competitively with matched cascades and offers clearer advantages under acoustic degradation, particularly in preserving high-level task semantics. These findings suggest that text-only fine-tuning of SLMs can serve as a practical adaptation strategy for voice agents when paired speech-target data are costly to collect.

</details>


<div id='cs.CR'></div>

# cs.CR [[Back]](#toc)

### [46] [Local LLM Agents as Vulnerable Runtimes:A Source-Code Audit of the Agent Runtime Layer](http://arxiv.org/abs/2606.21071v1)
*Zhengsong Zhang,Zongze Li,Jiawei Guo,Haipeng Cai*

Main category: cs.CR

Selection reason: 本地LLM agent安全审计，直接关注agent运行时安全，可迁移至coding agent安全控制

TL;DR: CLAWAUDIT是针对本地LLM代理运行时的静态审计框架，通过自定义规则将漏洞检测召回率从约20%提升至约70%，且规则泛化良好。


<details>
  <summary>Details</summary>
Motivation: 现有研究未检查代理实现层（提示构建器、解析器等）的安全边界，而这些组件存在实现级漏洞风险。

Method: 基于STRIDE推导五类别漏洞分类法，为Semgrep和CodeQL开发47条YAML规则和30条查询，并在包含446个漏洞的基准上评估。

Result: 在217个测试漏洞上，Semgrep召回率从21.7%提升至66.8%，CodeQL从13.8%提升至75.1%，训练/测试差距小于4个百分点。

Conclusion: CLAWAUDIT能有效检测本地LLM代理的漏洞，但召回率导向规则需人工分类，建议部署前增加语义过滤。

中文摘要：像OpenClaw和Nanobot这样的本地LLM代理在用户终端机器上运行，并通过自然语言目标对主机资源（shell、文件系统、浏览器、存储的凭证和消息应用程序）进行操作。这些代理已成为特权软件运行时，在用户意图、模型输出和主机级操作之间进行中介。现有研究通过提示注入、恶意技能、市场风险或黑盒评估来描述该领域。但实现该中介的实现层——提示构建器、解析器、工具调度器、技能加载器、内存写入器、网络客户端和权限门——一直是未检查的安全边界。据我们所知，没有先前工作检查代理的源代码树以审计这些组件的实现级安全弱点。我们提出CLAWAUDIT，一个用于测量本地LLM代理运行时漏洞暴露的静态审计框架。CLAWAUDIT从STRIDE推导出五类别漏洞分类法，并开发针对代理特定模式的自定义静态分析规则，这些模式在已有的漏洞分析规则集中不存在。我们将分类法实例化为两个后端、47个Semgrep YAML规则和30个CodeQL查询，并在OPENCLAWBENCH上评估，该基准包含来自OpenClaw仓库的446个源代码级建议，并临时分为229个规则推导（训练）和217个保留（测试）建议。在保留测试中，CLAWAUDIT将Semgrep召回率从21.7%（Pro基线）提升到66.8%，CodeQL召回率从13.8%（安全扩展）提升到75.1%。所有四种配置的训练/测试差距保持在4个百分点以内，表明规则推广到规则编写时未见过的漏洞。初步的实时代码审计显示，这些面向召回率的规则需要手动分类，这促使在生产部署前进行语义过滤。

Original abstract: Local LLM agents such as OpenClaw and Nanobot run on end-user machines and act on host resources - the shell, filesystem, browser, stored credentials, and messaging applications - through natural-language goals. These agents have become privileged software runtimes that mediate between user intent, model outputs, and host-level actions. Existing research characterizes the landscape through prompt injection, malicious skills, marketplace risks, or black-box evaluation of agents. But the implementation layer that performs this mediation, the prompt builder, parser, tool dispatcher, skill loader, memory writer, network client, and permission gate, has remained an unexamined safety boundary. To our knowledge, no prior work has examined the agent's source tree to audit these components for implementation-level security weaknesses. We present CLAWAUDIT, a static auditing framework for measuring vulnerability exposure in local LLM agent runtimes. CLAWAUDIT derives a five-category vulnerability taxonomy from STRIDE and develops custom static-analysis rules that target agent-specific patterns absent from established rule sets for vulnerability analysis. We instantiate the taxonomy in two backends, 47 Semgrep YAML rules and 30 CodeQL queries, and evaluate on OPENCLAWBENCH, a benchmark of 446 source-code-level advisories from the OpenClaw repository and split temporally into 229 rule-derivation (train) and 217 held-out (test) advisories. On the held-out test, CLAWAUDIT raises Semgrep recall from 21.7% (Pro baseline) to 66.8%, and CodeQL recall from 13.8% (security-extended) to 75.1%. Train/test gaps remain within 4 percentage points for all four configurations, indicating that the rules generalize to vulnerabilities unseen during rule writing. A preliminary live-code audit shows that these recall-oriented rules require manual triage, motivating semantic filtering before production deployment.

</details>


### [47] [Evaluating LLMs for Real-World Web Vulnerability Detection](http://arxiv.org/abs/2606.21397v1)
*Sebastian Neef,Luca Jungnickel,Antonio Benjamin Buchholz,Valene Spence,Vicente Birke Gonzalez*

Main category: cs.CR

Selection reason: LLM评估Web漏洞检测，涉及代码安全agent与prompt设计。

TL;DR: 对六种LLM在WordPress插件漏洞检测上的基准测试显示，模型和提示选择影响检测率，范围限定提示优于开放性提示，但一致性低，且无模型能检测所有漏洞。


<details>
  <summary>Details</summary>
Motivation: 探索LLM在真实网络漏洞检测（尤其是WordPress插件）中的有效性，回答模型和提示选择如何影响检测性能。

Method: 使用五种不同提示设计，对六个LLM（包括前沿和开放权重模型）在SQL注入、XSS等漏洞上进行三次迭代的静态分析基准测试。

Result: Claude Opus 4.6检测率最高（63%），MiniMax M2.5达48%，Qwen 3.5仅35%；范围限定提示优于开放性提示；模型报告一致性低（低至50%）；无模型识别出所有基线漏洞。

Conclusion: LLM在漏洞检测中有潜力但有限，提示设计至关重要，模型一致性差，需进一步研究；发布代码和数据支持未来工作。

中文摘要：大型语言模型（LLMs）已成为自动漏洞检测的有前途的工具，但其在网络特定漏洞方面的有效性仍有待探索。本工作对六种前沿模型（Claude Opus 4.6, Codex GPT-5.4, Gemini 3.1-pro-preview）和开放权重模型（Qwen 3.5, Qwen 3 Coder Next, MiniMax M2.5）在使用静态分析检测WordPress插件中的真实网络漏洞（包括SQL注入、存储型跨站脚本、路径遍历和远程代码执行）的能力进行了基准测试。通过使用五种不同结构、范围和复杂度的提示设计，并在三次实验迭代中，我们旨在回答模型和提示选择如何影响漏洞检测。我们的结果表明，所有模型都能检测到有效的安全问题，但检测率因模型和提示而异。例如，Claude Opus 4.6达到了最高的网络漏洞检测率（63%），而开放权重的MiniMax M2.5与其他前沿模型性能相当（48%），自托管的Qwen 3.5仅达到35%。我们表明，缩小漏洞范围的范围限定提示优于开放性提示，而提示复杂度影响不大。令人惊讶的是，没有模型在三次实验迭代中实现完全的报告一致性，有些甚至低至50%。我们的实验展示了基于LLM的漏洞检测的机遇和局限，因为没有模型正确识别出其中一个插件中的一个基线漏洞。此外，我们为安全从业人员总结了实用的经验教训，并发布了所有代码和数据以支持未来研究。

Original abstract: Large Language Models (LLMs) have emerged as a promising tool for automated vulnerability detection, yet their effectiveness on web-specific vulnerabilities remains to be explored.
  This work benchmarks six frontier (Claude Opus 4.6, Codex GPT-5.4, Gemini 3.1-pro-preview) and open-weight models (Qwen 3.5, Qwen 3 Coder Next, MiniMax M2.5) on their ability to detect real-world web vulnerabilities using static analysis in WordPress plugins, including SQL injection, stored cross-site scripting, path traversal, and remote code execution. Using five prompt designs of varying structure, scope, and complexity across three experiment iterations, we aim to answer how model and prompt choice affects vulnerability detection.
  Our results show that all models are capable of detecting valid security issues, but the detection rate varies depending on the model and prompt. For example, Claude Opus 4.6 achieved the highest web vulnerability detection rate (63%), while open-weight MiniMax M2.5 performs on par with other frontier models (48%), and self-hosted Qwen 3.5 only achieved 35%. We show that scoped prompts that narrow the vulnerability scope outperform open-ended ones, whereas the prompt complexity has little impact. Surprisingly, no model achieved full reporting consistency across three experiment iterations, with some as low as 50%. Our experiments demonstrate the opportunities and limits of LLM-based vulnerability detection, as no model correctly identified one baseline vulnerability in one of the plugins.
  Additionally, we derive practical lessons learned for security practitioners and publish all code and data to support future research.

</details>


### [48] [A Longitudinal Study of Android Apps Signing Key Protection](http://arxiv.org/abs/2606.21487v1)
*Mark Huasong Meng,Qing Zhang,Weirao Lu,Chunyang Chen*

Main category: cs.CR

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 通过挖掘公共仓库发现Android签名密钥大量泄露，影响超100亿用户，并演示了实际攻击，揭示系统性风险。


<details>
  <summary>Details</summary>
Motivation: 近期OEM厂商平台密钥泄露事件表明，即使签名机制设计良好，开发者疏忽仍会导致密钥泄露，威胁软件供应链安全。

Method: 纵向生态系统研究：挖掘GitHub仓库获取Android签名凭据，通过暴露密码恢复受损密钥，与超过4000个应用签名匹配，并进行概念验证攻击。

Result: 发现5,673个受损密钥库和26个唯一证书，关联278个真实世界应用（26个第三方和252个预装），影响超100亿用户；证明攻击可行并发现汽车平台溢出风险。

Conclusion: 签名密钥管理不善是系统性风险，需在Android发布工程和分发基础设施中加强密钥管理支持。

中文摘要：Android应用签名依赖于开发者管理的凭据，因此安全的密钥保护对于软件供应链的完整性至关重要。最近涉及两个主要OEM制造商的平台密钥泄露事件表明，即使设计再完善的签名机制也可能因开发人员的疏忽而受损。在这项工作中，我们进行了一项纵向生态系统研究，通过挖掘公共仓库中的Android签名凭据，通过暴露的密码恢复受损密钥，并将其与从主要应用商店和OEM系统镜像收集的4000多个应用的签名进行匹配，来表征这一威胁。我们的分析发现了GitHub上的5,673个受损密钥库和26个与278个真实世界应用关联的唯一证书。这些包括公共应用商店中的26个第三方应用和来自七家制造商的252个预装应用，共同影响超过100亿用户。我们通过概念验证的应用替换攻击演示了这些泄露的实际可利用性，并识别出非智能手机平台中的溢出风险，包括安装在超过1,100种车型中的流行汽车主机平台。我们的结果揭示了签名密钥管理不善是一个系统性风险，强调了在Android发布工程和分发基础设施中需要更严格的密钥管理支持。

Original abstract: Android app signing relies on developer-managed credentials, making secure key protection essential for the integrity of the software supply chain. A recent platform key leakage incident involving two major OEM manufacturers demonstrates that even robustly designed signing mechanisms can be compromised due to developers' oversight. In this work, we conduct a longitudinal ecosystem study to characterize this threat by mining public repositories for Android signing credentials, recovering compromised keys via exposed passwords, and matching them against signatures from over 4,000 apps collected from major stores and OEM system images. Our analysis identifies 5,673 compromised keystores on GitHub and 26 unique certificates linked to 278 real-world apps. These include 26 third-party apps in public app stores and 252 preinstalled apps from seven manufacturers, collectively affecting over 10 billion users. We demonstrate the practical exploitability of these leaks through a proof-of-concept app replacement attack and identify spillover risks in non-smartphone platforms, including a popular automotive head-unit platform installed in over 1,100 vehicle models. Our results reveal that signing-key mismanagement is a systemic risk, underscoring the need for a more rigorous key-management support in Android release engineering and distribution infrastructures.

</details>


### [49] [Safe to Check, Unsafe to Use: Relinking at the Compression Boundary of LLM Agents](http://arxiv.org/abs/2606.21732v1)
*Zesen Liu,Zihan Zhang,Dongdong She*

Main category: cs.CR

Selection reason: 揭示压缩边界漏洞，对agent安全防御有启发

TL;DR: 发现了一种名为relinking的压缩边界漏洞，攻击者通过将恶意载荷分散在长上下文中，使压缩器将其重新链接为恶意指令，现有防御无效，提出的Relink工具和KBRA防御分别实现了高攻击成功率和完全防御。


<details>
  <summary>Details</summary>
Motivation: LLM代理使用摘要压缩来缩短长上下文，但改变了安全边界，可能引入新的漏洞，需要识别和防御。

Method: 形式化定义对抗性relinking，设计基于DSL的自动化工具Relink将恶意载荷拆分为良性片段，提出KBRA防御机制。

Result: 在四个基准测试中，Relink达到86.9%的Relink率和后端动作率（对照组17.0%），KBRA防御将残余后端动作率降至0.0%。

Conclusion: Relinking是真实且危险的漏洞，现有防御不足，KBRA可有效防御，需在设计压缩系统时考虑此类攻击。

中文摘要：基于摘要的提示压缩越来越多地被LLM代理用于缩短长分布式上下文，但它改变了安全边界：过滤器检查压缩前的提示，而后端则对新生成的压缩上下文进行操作。我们发现了relinking，一种压缩边界漏洞，其中压缩器充当混淆代理，将分布式的、局部良性的片段总结成一个完整的恶意指令。与提示注入不同，relinking无需在源上下文中放置显式的恶意载荷。我们表明，relinking源于摘要本身：注意力使分离的片段联合可用，预训练使兼容的片段连接起来变得合理，后训练则倾向于生成紧凑的后端可操作摘要。我们将攻击者诱导的形式形式化为对抗性relinking，并提出了Relink，一种基于DSL的自动化工具，它将恶意载荷分割成良性片段，同时在压缩前保持完整载荷不存在。在四个长上下文代理基准测试中，Relink实现了86.9%的Relink率和后端动作率，而干净分割的对照组为17.0%。现有的防御措施无法可靠地捕获对抗性relinking；我们的KBRA防御将残余后端动作率降低到0.0%。

Original abstract: Summarization-based prompt compression is increasingly used by LLM agents to shorten long, distributed contexts, but it shifts the security boundary: filters inspect the pre-compression prompt while the backend acts on a newly generated compressed context. We identify relinking, a compression-boundary vulnerability where the compressor behaves as a confused deputy, summarizing distributed, locally benign fragments into a complete malicious instruction. Unlike prompt injection, relinking need not place an explicitly malicious payload in the source context. We show that relinking arises from summarization itself: attention makes separated fragments jointly available, pre-training makes compatible fragments plausible to connect, and post-training favors compact backend-actionable summaries. We formalize the attacker-induced form as adversarial relinking and present Relink, an automated DSL-based tool that splits malicious payloads into benign fragments while keeping the complete payload absent before compression. Across four long-context agent benchmarks, Relink achieves 86.9% Relink Rate and Backend Action Rate versus 17.0% for clean-split controls. Existing defenses fail to reliably capture adversarial relinking; our KBRA defense reduces residual Backend Action Rate to 0.0%.

</details>


<div id='cs.IR'></div>

# cs.IR [[Back]](#toc)

### [50] [When Does Overlap Help? OSU-Mem and a Cell-Conditional Analysis of Trajectory Memory for LLM Agents](http://arxiv.org/abs/2606.28376v1)
*Mellow Baixuan Chen,Xiangguo Sun*

Main category: cs.IR

Selection reason: 提出轨迹记忆中重叠语义单元组织方法，属于通用agent记忆机制，可迁移。

TL;DR: 提出OSU-Mem，通过将轨迹记忆组织成交叠语义单元（OSU）进行检索，发现交叠记忆仅在查询所需的证据步骤共享工具或实体时才有帮助，否则有害；通过合成和受控实验验证了该机制，并给出基于元数据的启发式预测方法。


<details>
  <summary>Details</summary>
Motivation: 长期跨度LLM智能体的交互轨迹容易超出提示预算，现有记忆方法要么丢失非局部证据要么保留模板损害决策质量，需要研究何时交叠记忆组织比扁平或不相交记忆更有效。

Method: 提出OSU-Mem，将轨迹记忆组织成交叠语义单元（OSU），通过预算约束的由粗到细扩展进行检索；并通过合成基准、τ-bench和ToolBench等实验，分析交叠记忆的条件性收益，提出基于元数据的启发式预测方法。

Result: 交叠记忆在证据步骤共享工具或实体时提升检索，在步骤完全异构时有害；总体优势在混合查询类型下消失，通过拆分查询和受控实验验证了机制；元数据可廉价估计共享性以预测交叠的效用。

Conclusion: 交叠记忆的收益是有条件的，依赖于证据步骤的共享结构；通过元数据启发式可预测其效果，为实际应用提供了指导；研究孤立了检索层，强调了机制性理解而非通用记忆系统。

中文摘要：长期跨度的大语言模型(LLM)智能体累积的交互轨迹会迅速超出任何实际的提示预算，而现有的记忆方法要么激进地截断并丢失非局部的证据，要么保留模板信息从而降低决策质量。我们探讨一个机制性问题而非声称一个更好的通用记忆系统：何时将轨迹记忆组织成交叠语义单元(OSU)——一组相关步骤，其中一步可能属于多个单元——比扁平或不相交的替代方案更有助于检索？我们通过OSU-Mem实例化这一想法，它通过预算约束的由粗到细扩展从交叠的OSU池中检索，并展示了其收益是有条件的：当查询所需的证据步骤共享工具调用或实体时，交叠记忆有帮助，但当这些步骤完全异构且不共享任何内容时则有害。在一个合成的基准测试中，证据根据构造携带这种共享结构，OSU-Mem按照理论预测优于最强基线；然而，在一个拼接的、构造的未增强τ-bench设置中，其相对于扁平检索的总体优势消失了。根据证据是否共享工具和实体来拆分查询显示，这种近乎平局是混合查询类型的假象，而非任一方法的属性；而ToolBench（一个为设计携带共享结构而构建的受控探针），通过交叠与不相交构造的对比（在覆盖引导变体下）证实了相同的机制，孤立了构造原则而非验证完整的默认系统。由于相关的共享性可以从元数据中廉价估计，该分析产生了一个基于元数据的启发式方法，用于预测交叠何时可能改善检索。我们特意孤立了检索层，通过检索质量和LLM中介的证据选择阶段进行评估。

Original abstract: Long-horizon large language model (LLM) agents accumulate interaction trajectories that quickly exceed any practical prompt budget, and existing memory methods either truncate aggressively and lose non-local evidence or retain boilerplate that degrades decision quality. We ask a mechanism question rather than claiming a better general-purpose memory system: when does organizing trajectory memory into overlapping semantic units (OSUs) -- groups of related steps in which one step may belong to several units -- help retrieval over flat or disjoint alternatives? We instantiate this in OSU-Mem, which retrieves from an overlapping OSU pool via budgeted coarse-to-fine expansion, and show its benefit is conditional: overlapping memory helps when the evidence steps a query needs share tool calls or entities, but hurts when those steps are fully heterogeneous and share neither. On a synthetic benchmark where evidence carries such shared structure by construction, OSU-Mem improves over the strongest baseline as the theory predicts; yet on a concatenated, constructed unaugmented $τ$-bench setting its aggregate advantage over flat retrieval vanishes. Splitting queries by whether their evidence shares tools and entities shows this near-tie to be an artifact of mixing query types rather than a property of either method, and ToolBench, a controlled probe built to carry shared structure by design, corroborates the same mechanism via an overlap-vs.-disjoint construction contrast (under a coverage-guided variant), isolating the construction principle rather than validating the full default system. Because the relevant sharing is cheaply estimable from metadata, the analysis yields a metadata-based heuristic for predicting when overlap is likely to improve retrieval. We deliberately isolate the retrieval layer, assessed by retrieval quality and an LLM-mediated evidence-selection stage.

</details>


### [51] [LEDGER: Scaling Agentic Document Editing with Dependency-aware Graph Retrieval](http://arxiv.org/abs/2606.28379v1)
*Mike Hang Wang,Utkarsh Garg,Reza Davari,Huitian Jiao,Hao Cheng,Baolin Peng,Tao Ge,Si-Qing Chen*

Main category: cs.IR

Selection reason: 文档编辑agent使用依赖图检索提高一致性，可迁移至代码agent的长程编辑

TL;DR: LEDGER通过构建轻量级依赖图实现局部编辑的上下文选择，在多种模型中提升一致性并减少计算开销。


<details>
  <summary>Details</summary>
Motivation: 解决智能体文档编辑中的上下文工程挑战，即高效进行局部编辑而不破坏文档的交叉引用和语义一致性。

Method: 构建轻量级依赖图显式建模文档结构（层次、显式引用、隐式依赖、语义关系），对每次编辑通过图引导检索只选择必要上下文。

Result: 在1.9k测试用例上，LEDGER将六种最先进模型的一致性从56%提升到76%，同时减少token使用；低推理努力的LEDGER匹配高推理努力的基线性能。

Conclusion: 显式依赖表示可以部分替代昂贵的内在推理，为智能体文档编辑提供高效方案。

中文摘要：我们提出LEDGER以应对智能体文档编辑中新颖的上下文工程挑战，其中对长结构化文档进行局部编辑必须高效进行，同时不破坏交叉引用或语义一致性。LEDGER构建了一个轻量级的依赖图，显式建模文档结构，包括层次组织、显式引用、隐式依赖和语义关系。对于每次编辑，图引导的检索仅选择必要的上下文，避免处理整个文档，同时保持一致性。我们在一个包含1.9k个测试用例的精心策划的基准上评估了LEDGER，涵盖各种文档类型和长度，跨越六个最先进的模型：LEDGER在全部六个模型和测试场景中将一致性从56%提高到76%，同时减少了token使用。值得注意的是，使用低推理努力的LEDGER在使用更少token的情况下达到了高推理努力的基线性能，表明显式依赖表示可以部分替代智能体文档编辑中昂贵的内在推理。

Original abstract: We introduce LEDGER to tackle the novel context engineering challenge of agentic document editing, where localized edits to long, structured documents must be applied efficiently without breaking cross-references or semantic consistency. LEDGER constructs a lightweight dependency graph that explicitly models document structure, including hierarchical organization, explicit references, implicit dependencies, and semantic relationships. For each edit, graph-guided retrieval selects only the necessary context, avoiding full-document processing while preserving consistency. We evaluate LEDGER on a curated benchmark of 1.9k test cases with various document types and lengths, spanning six state-of-the-art models: LEDGER improves consistency from 56% to 76% across all six models and test scenarios while reducing token usage. Notably, LEDGER with low reasoning effort matches baseline performance at high reasoning effort using fewer tokens, showing that explicit dependency representations can partially substitute for expensive internal reasoning in agentic document editing.

</details>


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [52] [SwarmX: Agentic Scheduling for Low-Latency Agentic Systems](http://arxiv.org/abs/2606.21401v2)
*Yeqi Huang,Yanwei Ye,Guomin Chen,Wenhao Su,Bin Gong,Jialian Li,Zhan Lu,Yangshen Deng,Xuan Sun,Le Xu,Luo Mai*

Main category: cs.DC

Selection reason: Agent调度系统SwarmX，降低延迟，可迁移至agent基础设施。

TL;DR: SwarmX通过调度特定的神经预测器和调度器-智能体框架，为自主AI应用实现低延迟调度，显著降低尾延迟并提高吞吐量。


<details>
  <summary>Details</summary>
Motivation: 自主AI应用的推理时间和模型调用结构依赖于提示语义，传统调度方法难以满足低延迟服务需求。

Method: 提出SwarmX系统，使用调度特定的神经预测器捕获提示、设备、运行时和目标模型特征，将分布预测暴露给路由器和缩放器，并集成到调度器-智能体框架中，支持预测器训练和在线自适应。

Result: 在生产部署（近一千GPU和百万CPU核心）和128-GPU测试台上，SwarmX在多智能体工作流中将尾延迟降低高达61.5%，吞吐量提升至2倍。

Conclusion: SwarmX有效解决了自主AI应用的调度挑战，实现了低延迟和高吞吐量，且易于集成到现有基础设施。

中文摘要：自主AI应用组合多个模型调用和工具执行，给GPU-CPU集群带来了新的调度挑战。它们的推理时间和模型调用结构通常依赖于提示语义，使得传统调度方法在低延迟服务中效果不佳。本文提出了SwarmX，一个为低延迟自主应用实现自主调度的系统。SwarmX使用调度特定的神经预测器来捕获提示、设备、运行时和目标模型特征；将分布预测暴露给路由器和缩放器以进行尾延迟感知决策；并提供预测器训练和在线自适应的机制。这些预测器和机制被集成到一个调度器-智能体框架中，为与现有调度和模型服务基础设施的集成提供了通用基础。我们使用生产部署（近一千个GPU和一百万个CPU核心）以及在128-GPU测试台上的受控实验评估了SwarmX。在多智能体代码生成、深度研究和多模态自主工作流中，与最先进的调度器相比，SwarmX将尾延迟降低了高达61.5%，并在相同的SLO下维持了高达生产调度器2倍的吞吐量。

Original abstract: Agentic AI applications compose multiple model calls and tool executions, creating new scheduling challenges for GPU-CPU clusters. Their inference time and model-call structure often depend on prompt semantics, making conventional scheduling approaches ineffective for low-latency serving. This paper presents SwarmX, a system that implements agentic scheduling for low-latency agentic applications. SwarmX uses scheduling-specific neural predictors to capture prompt, device, runtime, and target-model features; exposes distributional predictions to routers and scalers for tail-aware decisions; and provides mechanisms for predictor training and online adaptation. These predictors and mechanisms are integrated into a scheduler-agent framework that provides a common substrate for integration with existing scheduling and model-serving infrastructure. We evaluate SwarmX using production deployment (nearly one thousand GPUs and one million CPU cores) and controlled experiments on a 128-GPU testbed. Across multi-agent code generation, deep research, and multimodal agentic workflows, SwarmX reduces tail latency by up to 61.5% compared to state-of-the-art schedulers and sustains up to 2x the throughput of production schedulers under the same SLO.

</details>


### [53] [Bridging Design and Execution: A Visual Graph Editor for Edge and Cloud Workflows](http://arxiv.org/abs/2606.21416v1)
*Katarina-Glorija Grujić,Nikola Stanković,Maja Vukasović,Miloš Simić*

Main category: cs.DC

Selection reason: cs.SE 论文按全量策略收录。

TL;DR: 提出一个可视化图形编辑器，用于建模边缘/云模块化应用，支持内核、共享内存和事件触发器，自动序列化为JSON/XML，可直接部署，并通过联邦学习案例验证。


<details>
  <summary>Details</summary>
Motivation: 现有工具缺乏对边缘/云场景下数据共享、执行顺序和依赖关系的明确建模支持，需要一种统一的可视化方法来桥接设计时与运行时。

Method: 设计领域特定图形编辑器，提供三类抽象（内核、共享内存节点、事件触发器），支持可视化图构建、属性配置和连接，自动生成JSON/XML表示并通过执行API部署。

Result: 编辑器生成的图形模型能明确表达执行语义和模块性，支持直接部署；联邦学习案例展示了其实际应用效果。

Conclusion: 该工作为分布式环境中模块化计算的视觉编排奠定了基础，具有可扩展性，适用于未来复杂分布式应用。

中文摘要：设计用于边缘和云计算环境的模块化应用涉及协调多个计算内核、共享数据和事件驱动执行。本文提出了一种领域特定的可视化图形编辑器，使用户能够通过统一接口建模此类应用。该编辑器支持三种第一类抽象：内核（代表计算单元）、共享内存节点（建模分布式数据）以及事件触发器（捕获执行依赖）。用户可以可视化地构建图形、配置节点属性并连接元素以定义数据流和控制流。生成的图形自动序列化为机器可读表示（JSON/XML），并可传递给执行API，从而弥合设计时建模与运行时部署之间的差距。该编辑器的图形化模型改善了对数据共享、执行顺序和依赖关系的推理，特别是在分布式边缘和云场景中。为展示其适用性，我们讨论了一个联邦学习工作流，其中本地训练内核通过事件驱动协调与共享全局模型交互。与传统的流程编辑器和通用图表工具相比，所提出的系统提供了明确的执行语义、模块化和直接可部署性。这项工作为分布式环境中模块化计算的视觉编排奠定了基础，并提供了对用户自定义内核、事件类型和替代执行后端的可扩展性，从而支持未来对复杂分布式应用的探索。

Original abstract: Designing modular applications for edge and cloud computing environments involves coordinating multiple computational kernels, shared data, and event-driven execution. This paper presents a domain-specific visual graph editor that enables users to model such applications using a unified interface. The editor supports three first-class abstractions: kernels, representing computational units; shared memory nodes, modeling distributed data; and event triggers, capturing execution dependencies. Users can construct graphs visually, configure node properties, and connect elements to define data and control flow. The resulting graphs are automatically serialized into machine-readable representations (JSON/XML) and can be passed to an execution API, bridging the gap between design-time modeling and runtime deployment. The editor's graph-based model improves reasoning about data sharing, execution order, and dependencies, particularly in distributed edge and cloud scenarios. To demonstrate its applicability, we discuss a federated learning workflow, where local training kernels interact with a shared global model through event-driven coordination. Compared to traditional workflow editors and general-purpose diagramming tools, the proposed system provides explicit execution semantics, modularity, and direct deployability. This work lays the foundation for visual orchestration of modular computation in distributed environments and offers extensibility for user-defined kernels, event types, and alternative execution backends, enabling future exploration of complex distributed applications.

</details>


<div id='cs.GT'></div>

# cs.GT [[Back]](#toc)

### [54] [Multiscale Exit-Join Dynamics: Tactical Consensus and Strategic Coalition Formation](http://arxiv.org/abs/2606.26139v1)
*Quanyan Zhu*

Main category: cs.GT

Selection reason: 多智能体博弈论模型，对多agent协作策略有潜在启发

TL;DR: 该论文通过耦合战略退出-加入决策与战术共识动态，构建了多尺度联盟形成模型，揭示了低切换壁垒导致的不稳定-共识悖论，并建立了均衡存在性结果。


<details>
  <summary>Details</summary>
Motivation: 为理解联盟形成中战略与战术层面的相互作用提供统一框架，并解释切换壁垒对共识与稳定的影响。

Method: 建立快慢架构，将DeGroot共识过程（快）与激励驱动的联盟重组（慢）结合，通过固定点理论刻画联合战术-战略均衡。

Result: 发现了不稳定-共识悖论：低或负切换壁垒阻止战略收敛但促进全球战术共识；给出了均衡存在性条件和固定点特征。

Conclusion: 该模型统一了联盟形成、共识动态、信息聚合与战略稳定性，为多智能体系统提供了分析工具。

中文摘要：本文开发了一个多尺度联盟形成模型，其中战略性的退出和加入决策与联盟内部的战术共识动态相耦合。联盟价值通过联盟内部的信息聚合内生生成，而Aumann-Dreze收益、转换摩擦和接受规则则支配着战略重构。该框架引入了一种快慢架构，其中可转移的联盟价值从DeGroot式的共识过程中产生，而联盟结构则通过激励驱动的退出-加入动态演化。分析刻画了联合战术-战略均衡、战术和战略一致性的条件、隔离、两极分化以及维持稳定联盟结构的认知障碍。建立了耦合动力学的固定点特征和存在性结果。数值实验揭示了一种不稳定-共识悖论：低或负的转换壁垒可能阻止战略收敛，同时促进足以实现全球战术共识的时间混合。结果提供了一个关于多智能体系统中联盟形成、共识动态、信息聚合和战略稳定性的统一视角。

Original abstract: This paper develops a multiscale model of coalition formation in which strategic exit-and-join decisions are coupled with tactical consensus dynamics inside coalitions. Coalition value is generated endogenously from within-coalition information aggregation, while Aumann-Dreze payoffs, switching frictions, and acceptance rules govern strategic reconfiguration. The framework introduces a fast-slow architecture in which transferable coalition value emerges from DeGroot-style consensus processes, while coalition structures evolve through incentive-driven exit-and-join dynamics. The analysis characterizes joint tactical-strategic equilibria, conditions for tactical and strategic unanimity, segregation, polarization, and cognitive barriers that sustain stable coalition structures. A fixed-point characterization and existence results are established for the coupled dynamics. Numerical experiments reveal an instability-consensus paradox: low or negative switching barriers may prevent strategic convergence while simultaneously promoting temporal mixing sufficient to achieve global tactical consensus. The results provide a unified perspective on coalition formation, consensus dynamics, information aggregation, and strategic stability in multi-agent systems.

</details>
